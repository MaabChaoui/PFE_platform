# HANDOFF — AKN-RLM Viva Demo build

Single source of truth between implementation sessions. **Full design: [`plan.md`](plan.md)** (architecture, API, control map, data contracts §6, all 16 sessions). Read `plan.md` first, then the latest entry below.

Demo for the ENSIA viva on **13/06/2026**. Goal: a polished, interactive web demo of AKN-RLM (not a chatbot) — new `backend/` (FastAPI wrapping `akn_rlm`) + `frontend/` (Next.js), via `docker-compose`. **Offline-first**: precomputed predictions/metrics drive Results, Benchmark, and Main-page replay with no LLM; live is an enhancement.

## Quick reference (verify paths before relying on them)
- Backend `:8000` (`/api/...`) · Frontend `:3000` (`NEXT_PUBLIC_API_BASE=http://localhost:8000/api`).
- Python pkg: `akn_rlm/akn_rlm/` · UI entry point: `akn_rlm/akn_rlm/api/answer.py` (`answer_query`, `AnswerResponse`) · dispatcher: `akn_rlm/akn_rlm/rlm/dispatcher.py` (`build_dispatcher(...)`).
- Indices (present): `akn_rlm/data/indices/{bm25.pkl,dense.faiss,dense_meta.parquet}`.
- Dataset: `latest_dataset/{akn,txt,rdf,pdf}/` + `latest_dataset/AlgerianLegalBench_v3.0_final.json`. KG: `latest_dataset/rdf/algerian_legal_kg.ttl` (74 MB; per-law `.ttl` files are small).
- Precomputed (locked run): `akn_rlm/eval_results/rlm_dispatched_full_phase_e_final/{metrics.json,predictions.jsonl}`. Classifier eval: `akn_rlm/eval_results/classifier_accuracy_llm_final/`.
- **Dataset env vars the backend must set** (config auto-discovery does NOT find `latest_dataset/`): `AKN_RLM_AKN_DIR`, `AKN_RLM_KG_PATH`, `AKN_RLM_BENCHMARK_PATH`.
- Data contracts (benchmark question, predictions record, `AnswerResponse`, KG subgraph, metrics): **`plan.md` §6** — embed the relevant one verbatim in each session's HANDOFF entry.
- Constraints: change `akn_rlm/` only **additively** (new telemetry behind a flag) so thesis metrics never drift. Every session must be **offline-verifiable** (no live LLM).

## Entry format (append one per session, ≤1000 tokens)
`## <Sxx> <title> (<YYYY-MM-DD>)` — **Status** done/partial · **Shipped** (files + endpoints/components) · **Contracts/paths** touched · **Verify** (exact offline commands) · **Gotchas** · **Next** (session id + first concrete step).

---

## Demo Build

## S0 — Repo + compose skeleton (2026-06-01)

- **Status:** done
- **Shipped:**
  - `backend/app/__init__.py`, `backend/app/main.py`, `backend/app/settings.py`
  - `backend/app/routers/__init__.py`, `backend/app/routers/health.py`
  - `backend/requirements.txt` (`fastapi`, `uvicorn[standard]`, `pydantic-settings` only)
  - `backend/Dockerfile` (`python:3.11-slim`; Python healthcheck, no curl needed)
  - `backend/README.md`
  - `frontend/app/layout.tsx` (persistent top nav, 7 links), `frontend/app/globals.css`
  - `frontend/app/page.tsx` (client component — health badge with `useEffect`)
  - `frontend/app/{architecture,corpus,kg,benchmark,results,about}/page.tsx` (6 placeholders)
  - `frontend/lib/api.ts` (`getHealth()`, `HealthResponse` type)
  - `frontend/package.json` (Next.js 14.2.29, Tailwind v3.4.17, TypeScript)
  - `frontend/{next.config.js,tsconfig.json,tailwind.config.ts,postcss.config.js,.env.local}`
  - `frontend/Dockerfile` (node:20-alpine multi-stage; `ARG NEXT_PUBLIC_API_BASE` before build; runs `node server.js`)
  - `docker-compose.yml` (two services on `demo` network; read-only mounts; Python healthcheck)
  - `.env.example` (all env vars documented)

- **Contracts/paths:**
  - Health JSON shape: `{"status":"ok","offline_mode":bool,"indices_present":bool,"dataset_present":bool,"predictions_present":bool,"llm":"unchecked"}`
  - `indices_present` = `INDICES_DIR/bm25.pkl` exists · `dataset_present` = `AKN_RLM_BENCHMARK_PATH` exists · `predictions_present` = `PREDICTIONS_PATH` exists
  - Settings env vars → local default → container override:
    - `AKN_RLM_AKN_DIR` → `<repo>/latest_dataset/akn` → `/data/latest_dataset/akn`
    - `AKN_RLM_KG_PATH` → `<repo>/latest_dataset/rdf/algerian_legal_kg.ttl` → `/data/latest_dataset/rdf/algerian_legal_kg.ttl`
    - `AKN_RLM_BENCHMARK_PATH` → `<repo>/latest_dataset/AlgerianLegalBench_v3.0_final.json` → `/data/latest_dataset/AlgerianLegalBench_v3.0_final.json`
    - `INDICES_DIR` → `<repo>/akn_rlm/data/indices` → `/data/indices`
    - `EVAL_RESULTS_DIR` → `<repo>/akn_rlm/eval_results` → `/data/eval_results`
    - `PREDICTIONS_PATH`, `METRICS_PATH` → derived from `EVAL_RESULTS_DIR/rlm_dispatched_full_phase_e_final/` when unset (model_validator ensures Docker override of `EVAL_RESULTS_DIR` propagates)
  - Read-only mount convention: `./latest_dataset → /data/latest_dataset`, `./akn_rlm/data/indices → /data/indices`, `./akn_rlm/eval_results → /data/eval_results`
  - Ports: backend `:8000`, frontend `:3000` · `NEXT_PUBLIC_API_BASE=http://localhost:8000/api`

- **Verify:**
  ```bash
  # Local backend
  cd backend && pip install -r requirements.txt
  uvicorn app.main:app --port 8000   # from backend/ dir
  curl -s localhost:8000/api/health
  # Expected: {"status":"ok","offline_mode":true,"indices_present":true,"dataset_present":true,"predictions_present":true,"llm":"unchecked"}

  # Local frontend
  cd frontend && npm install && npm run build   # build must succeed
  npm run dev   # open http://localhost:3000 — nav has 7 links; health badge shows all flags true

  # Docker (from repo root)
  docker-compose up --build
  curl -s localhost:8000/api/health   # same JSON as above
  # http://localhost:3000 loads; health badge shows "ok" + all flags true
  ```
  **Observed (local):** `indices_present=true`, `dataset_present=true`, `predictions_present=true` — all three flags confirmed true (not just 200). `next build` produced 7 routes, 0 errors.

- **Gotchas:**
  - `NEXT_PUBLIC_API_BASE` is inlined at build time, not runtime. The compose value is `http://localhost:8000/api` (host-visible) — NOT `http://backend:8000`. Browser runs on host, not inside the compose network.
  - Tailwind pinned to v3 (`^3.4.17`). npm@11+ installs v4 by default; v4 uses `@tailwindcss/postcss` and breaks the `tailwind.config.ts` + `postcss.config.js` layout.
  - Repo root derived from `Path(__file__).resolve().parents[2]` in `settings.py`. Verified depth: settings.py→app→backend→repo. Running uvicorn from any CWD gives correct path defaults.
  - `python:3.11-slim` has no curl; healthcheck uses Python `urllib.request` one-liner.
  - Frontend standalone output: `node server.js` (not `next start`); `.next/static` and `public/` must be copied alongside `.next/standalone/`.

- **Next:** S1 — Backend PipelineService + corpus service + meta/corpus endpoints. First step: add `akn_rlm` editable install + `rdflib`, `rank-bm25`, `sentence-transformers`, `faiss-cpu` to `backend/requirements.txt` and Dockerfile; create `backend/app/services/pipeline.py` with singleton `PipelineService`.

## S1 — Backend PipelineService + corpus API (2026-06-02)

- **Status:** done
- **Shipped:** `backend/app/services/pipeline.py`, `services/corpus.py`, `deps.py`, `models/{__init__,corpus}.py`, `routers/corpus.py`; updated `main.py`, `settings.py`, `health.py`, `backend/requirements.txt`, `backend/Dockerfile`, `docker-compose.yml`; fixtures `backend/fixtures/{akn,txt,benchmark,rdf}`; tests `backend/tests/test_corpus_api.py`. Endpoints: `GET /api/meta`, `/api/corpus/documents`, `/api/corpus/documents/{doc_id}`, `/xml`, `/text`, `/api/corpus/articles/{doc_id}/{article_ref}`, `/api/corpus/search`.
- **Contracts/paths:** `PipelineService` warms `parse_all()` + `ArticleRegistry` once at lifespan startup and keeps per-doc/per-article in-memory indexes; `bm25()`, `dense()`, `llm_pool()`, `kg()` are memoized lazy loaders, `get_dispatcher()` is stubbed with `NotImplementedError("wired in S4")`. Uses `Article{doc_id, article_ref, text_ar, text_normalized, frbr_uri, ancestors, eid, filename_stem, doc_title, doc_date, doc_type, paragraphs}` and `DocEntry{canonical_id, doc_title, doc_date, doc_type, filename_stem, article_eids, article_refs}`. Doc lookups resolve direct then `registry.resolve_alias`; raw files use `filename_stem -> AKN_RLM_AKN_DIR/*.xml` and `AKN_RLM_TXT_DIR/*.txt`. New vars: `AKN_RLM_TXT_DIR`, `AKN_RLM_RDF_DIR`, `AKN_RLM_PDF_DIR`, `AKN_RLM_EXTRACTION_REPORT_PATH`. Local importability: `pip install -e ./akn_rlm`; Docker importability: `./akn_rlm:/app/akn_rlm:ro` plus `PYTHONPATH=/app/akn_rlm`. Meta counts: corpus parse/registry, benchmark JSON `questions`, RDF `extraction_report.summary.total_triples`.
- **Verify:** `/home/maab/Documents/pfe/methodology/PFE_locally/.venv/bin/python -m pip install -e ./akn_rlm && /home/maab/Documents/pfe/methodology/PFE_locally/.venv/bin/python -m pip install -r backend/requirements.txt` succeeded. `cd backend && $PY -m uvicorn app.main:app --port 8000`: `/api/meta -> {"documents":45,"articles":8868,"benchmark_questions":244,"kg_triples":765215,...}`; `/api/corpus/documents | jq length -> 45`; family-code detail returned 239 articles and 235 with non-empty ancestors; article `84-11_1984-06-09/1` returned Arabic text; `/xml` and `/text` returned raw content; URL-encoded search `q=الزواج -> 44`; alias `75-8_1975-09-26 -> 75-58_1975-09-26`. `$PY -m pytest backend/tests -m "not live" -q -> 3 passed`. `docker-compose up --build` succeeded after freeing disk; backend healthy; container `/api/meta` matched local counts.
- **Gotchas:** current `parse_all()` returns 8868 Article objects, not the brief’s approximate 9002. One FRBR collision maps `06-01_2006-02-20` to `05-01_2005-02-06`; the wrapper preserves benchmark filename identities so the API still exposes 45 docs. Raw Arabic in curl URLs must be URL-encoded (`curl -G --data-urlencode`). Starlette `TestClient` hangs inside the sandbox; tests pass outside it.
- **Next:** S2 — benchmark + results services (precomputed). First step: `services/benchmark.py` loading `AKN_RLM_BENCHMARK_PATH` + joining `…/rlm_dispatched_full_phase_e_final/predictions.jsonl` by `question_id`.

## S2 — Benchmark + results services (2026-06-02)

- **Status:** done
- **Shipped:** `backend/app/services/{benchmark.py,results.py}`, `routers/{benchmark.py,results.py}`, `models/{benchmark.py,results.py}`, updated `deps.py`, `main.py`, `settings.py`; `backend/app/data/baselines.json`; fixtures under `backend/fixtures/eval/`; tests `backend/tests/{conftest.py,test_benchmark_api.py,test_results_api.py}`. Endpoints: `GET /api/benchmark/questions`, `/api/benchmark/questions/{id}`, `/api/benchmark/stats`, `/api/results/metrics`, `/api/results/baselines`, `/api/results/runs`, `/api/results/classification`.
- **Contracts/paths:** benchmark top-level `{metadata,document_registry,questions[]}` loaded from `AKN_RLM_BENCHMARK_PATH`; predictions loaded from `PREDICTIONS_PATH` and joined by `question_id`. Per-question citation correctness uses set overlap of `gold_article_ids` vs `pred_article_ids` (`doc_id#art_N`) into `n_gold,n_pred,n_correct,precision,recall,f1`; abstention questions use `query_type=="unanswerable"` / `gold_abstain` and score `predicted_abstain==gold_abstain`. Gold text enrichment calls S1 `corpus.article(document_id, article_ref)` so aliases resolve; unresolved or `in_dataset:false` returns `text:null,resolved:false`. `metrics()` serves `METRICS_PATH` structure as-is. `runs()` scans `EVAL_RESULTS_DIR/*/metrics.json`, locks `rlm_dispatched_full_phase_e_final`. New `CLASSIFIER_DIR` derives from `EVAL_RESULTS_DIR/classifier_accuracy_llm_final`; classification computes accuracy/per-type P/R/F1 from `classifier_predictions.jsonl` gold/pred labels and serves `confusion_matrix.json` verbatim (true→pred). `baselines.json` keys: improvement factors, tier1/2 tables, phase progression, ablation, notes, metric definitions.
- **Verify:** `$PY -m pytest backend/tests -m "not live" -q` -> `9 passed, 82 warnings`. Host `cd backend && $PY -m uvicorn app.main:app --port 8000`: `questions?page_size=5 -> 244`; `query_type=unanswerable -> 40`; stats query_type = 66/59/40/26/17/17/12/7; `civ_ra_q01` has 2 resolved gold texts, 2 predicted citations, 9 trajectory steps, `n_correct=1`; metrics `citation_f1=0.3044757580895478`, `abstention_f1=0.7034482758620689`; baselines improvement factors `{1.64,2.9,1.74}`; runs length `87` with locked flagged; classification `accuracy=0.6967213114754098`, 8 labels. `docker-compose up --build` rebuilt backend/frontend, backend health passed, same endpoint numbers worked in-container.
- **Gotchas:** local sockets/TestClient need running outside the sandbox. `civ_ra_q01` expected docs use alias `75-8_1975-09-26` but diff IDs are canonical `75-58_1975-09-26#art_N`. Gold text resolution: 431/442 in-dataset expected articles resolved; 11 unresolved: `env_un_q01 03-10/52`, `env_ea_q01 03-10/15`, `env_ra_q01 03-10/52`, `env_cd_q01 03-10/18`, `env_ra_q02 03-10/25`, `hous_ea_q01 11-04/5`, `hous_ra_q01 11-04/27`, `ip_ea_q01 03-05/54`, `ip_cd_q01 03-05/21`, `ip_ra_q01 03-05/151`, `xdom_mh_q06 03-10/18`. Results page should display locked 0.3045 vs gold-typed 0.313 from baselines notes.
- **Next:** S3 — KG index builder + subgraph API. First step: `backend/app/scripts/build_kg_index.py` that parses per-law TTLs in `latest_dataset/rdf/` into compact SQLite (`nodes`,`edges`), idempotent at entrypoint.

## S3 — KG index + subgraph API (2026-06-02)

- **Status:** done
- **Shipped:** `backend/app/scripts/build_kg_index.py`, `services/kg.py`, `routers/kg.py`, `models/kg.py`, updated `settings.py`, `deps.py`, `main.py`, `models/__init__.py`, `backend/entrypoint.sh`, `backend/Dockerfile`, `docker-compose.yml`, fixture TTLs `backend/fixtures/rdf/{94-03_1994-04-11.ttl,15-20_2015-12-30.ttl}`, tests `backend/tests/test_kg_api.py`. Endpoints: `GET /api/kg/meta`, `/api/kg/subgraph`, `/api/kg/node?id=...` and `/api/kg/node/{id:path}`, `/api/kg/search`.
- **Contracts/paths:** SQLite at `KG_INDEX_PATH` (`<repo>/backend/data/kg_index.sqlite`, Docker `/app/data/kg_index.sqlite`) with `nodes(id,type,label,doc_id,article_ref,article_eid,text,props_json)`, `edges(id,source,target,predicate)`, `build_info(key,value)` plus indices on edge source/target/predicate and node type/doc/label/text. Builder version `s3_kg_index_v2` parses one per-law TTL at a time from `AKN_RLM_RDF_DIR`, skips `algerian_legal_kg.ttl`, derives node/edge ontology from actual triples, and removes stale/forced generated SQLite before rebuild to handle low disk. URI law pattern `/law/<DATE>/<NUM>#<eid>` -> `doc_id=NUM_DATE` (dedupes malformed `NUM_DATE_DATE`), `article_eid=eid`, `article_ref=eid` without `art_`. Entry point runs `python -m app.scripts.build_kg_index` when `KG_BUILD_ON_START=true`; compose uses writable `kg_index:/app/data` and 180s backend health start period. Query paths use SQLite only; `rdflib` import is confined to the builder.
- **Subgraph contract:** `subgraph(seed?,doc_id?,node_types?,edge_types?,depth=1|2,limit<=600)` does bounded bidirectional BFS, seeds explicit URI or canonical doc Law node, applies filters to expansion, caps nodes, and returns `{nodes:[{id,type,label,doc_id,article_ref,props}],edges:[{id,source,target,predicate}],truncated,total_neighbors}`. Missing index returns `503 {"detail":"kg index not built; run build_kg_index"}`.
- **Verify:** local build from `backend/`: `$PY -m app.scripts.build_kg_index` -> `n_files=45 n_nodes=80560 n_edges=398335 seconds=26.94`; rerun -> skip; `--force` -> rebuild `seconds=26.89`. Host API: meta -> `{types:39,preds:69,totals:{nodes:80560,edges:398335}}`; `doc_id=84-11_1984-06-09&depth=1&limit=50` -> `{n:50,e:136,truncated:true,total_neighbors:763}`; search `q=الزواج&limit=10` returned hits; Article node `https://legal.dz/resource/law/1984-06-09/84-11#art_13` -> `corpus_link {84-11_1984-06-09,13}`, degree in/out `20/12`, props present. `rg rdflib backend/app --glob '!scripts/build_kg_index.py'` returned none. `$PY -m pytest backend/tests -m "not live" -q` -> `13 passed, 110 warnings`. Docker: first/stale boot rebuilt index in volume (`seconds=27.62`), health passed, host `/api/kg/meta` matched counts; second `docker-compose up --build` printed skip for v2 and meta still worked.
- **Gotchas:** node IDs are full URIs; prefer `/api/kg/node?id=...` with URL encoding. `rdf:type` is indexed as predicate `type` and dominates edge counts. Non-`/law/` URIs (`order`, `constitution`, `dzterm`, `dzorg`) intentionally have null corpus link/doc fields unless they also match the law regex. High-degree Law seeds truncate quickly; UI should surface `truncated` and `total_neighbors`. Local filesystem is nearly full, so builder deletes stale/forced generated SQLite before rebuild.
- **Next:** S4 — live answer + SSE + retrieve telemetry + classify + pipeline config/reset. First step: implement `PipelineService.get_dispatcher(options)` (memoized, lock-serialized, mirroring `akn_rlm/api/answer.py:_build_dispatcher`) and a `POST /api/answer` that wraps it (live-only; keep offline fallback in mind for S15).

## S4 — Live answer + SSE + classify + config/reset (2026-06-02)

- **Status:** done. Offline paths fully verified; live verified against the hardcoded internal AI-Grid endpoint (unreachable from this dev box) → confirmed graceful degradation (no crash). No `akn_rlm` edits.
- **Shipped:** `backend/app/models/answer.py`, `services/answer_runtime.py`, `routers/answer.py`, `backend/pytest.ini`; updated `services/pipeline.py` (`get_dispatcher`/`router`/`reset_dispatchers`/`answer`/`classify`/`_scoped_env` + `PipelineLiveError`), `models/__init__.py`, `main.py`, `requirements.txt` (+`sse-starlette==2.1.3`, `starlette>=0.40,<0.42` — 3.x pulls starlette 1.x which breaks fastapi 0.115.6); tests `tests/{test_answer_api.py,test_pipeline_config.py}`. Endpoints (`/api`): `POST /answer`, `POST /answer/stream` (SSE), `POST /classify`, `GET /pipeline/config`, `POST /pipeline/reset`. `get_dispatcher(options)` is memoized per normalized options key, lock-serialized over the whole build (env enhancer flags are process-global), reuses the shared `bm25/dense/llm_pool/router/registry` singletons; `AnswerOptions()` default reproduces `_build_dispatcher` exactly.
- **Contracts/paths:** `AnswerOptions` → backend mapping. **kwargs:** `enable_recursion`,`recursion_max_depth`,`enable_corrective_retry`,`enable_pervasive_adu`,`adu_extract_top_n`,`enable_ceiling_breakers`(=`ceiling_breakers`, request-safe kwarg — NOT env),`long_context_timeout_s`,`sub_model`,`recursion_coverage_min_overrides={multi_hop,rule_application:mh_ra_coverage_min}`,`kg_loader`(present iff `use_kg`). **env (set under lock, build, restore):** `AKN_E4_HYDE="1"/"0"`(=`hyde`); `AKN_E{1,2,3,5,6,7}_*="1"/"0"`(=`enhancers.eN`); `AKN_ENHANCERS="0"` (neutralize master, else it force-enables all); `AKN_NO_CITATION_GATE` is **presence-based** (`if os.getenv`) so gate-ON=**unset**, gate-OFF=`"1"`. `query_type` is a run-time `run(query, query_type=…)` arg, **excluded** from the build/memo key. Build env-flag truthiness = `enhancers._env_flag` = `{1,true,yes,on}`. **One converter** `answer_runtime.build_answer_response(query,raw,latency)` reuses `answer.py` `_to_citation`/`_summarise_step`/`_format_references`/`_HANDLER_LABELS_AR` for **live(auto)+manual+replay** (byte-identical, no drift). Replay = `replay_response(record)` adapts a precomputed `predictions.jsonl` record (`predicted_citations`/`predicted_abstain`, flat telemetry) → live `raw` shape. **SSE events:** `step`(`{index,step,depth,summary,detail}`) · `heartbeat`(live, every 5s while blocking) · `answer`(`AnswerResponseModel` dict; replay adds `scores={hcr,jir,answer_faithfulness,citation_groundedness,am_faithfulness_score}`) · `error`(`{detail}`) · `done`(`{ok,mode,n_steps}`). Replay is offline (404 on unknown `question_id`); live runs `answer()` in a threadpool (post-hoc step replay; mid-run streaming = explicit non-goal). **`error` is stream-terminal — it emits NO trailing `done`** (only replay/success end in `done`); S10 must treat `error` as the end of the stream. **Per-channel retrieve telemetry: DEFERRED to S5** (no `akn_rlm` edit; live/replay step view uses the dispatcher's existing rich trajectory). **Graceful-503:** `answer()`→`PipelineLiveError`→503 when `disp.run` raises, or returns an infra-failure envelope (`_telemetry.error`/reason∈{dispatch_*,empty_query}), or (heuristic) a **non-abstaining empty** result (no answer_text + no citations) — genuine domain abstentions (e.g. `no_verified_articles`,`unanswerable`) stay **200**. `classify()` uses `llm_classify(...,fallback_to_regex=False)`: confidence≥0.95→`{query_type,confidence}` else 503 (no regex guess masquerading as the LLM classifier).
- **Verify (offline, host `cd backend && $PY -m uvicorn app.main:app --port 8000`):** `$PY -m pytest backend/tests -m "not live" -q` → **20 passed, 3 deselected** (run outside sandbox). `/api/pipeline/config` → `options=19`, `query_types`=the 8, defaults match locked (`recursion_max_depth 3, hyde true, mh_ra_coverage_min 4, citation_gate true`). SSE replay `civ_ra_q01` → **9 `step` + 1 `answer` + 1 `done`** (no LLM). `/api/pipeline/reset` → `{ok:true,cleared:0}`. S0–S3 endpoints (`/api/{meta,corpus/documents,benchmark/stats,kg/meta,results/metrics}`) all 200. **Live (endpoint unreachable here):** `/api/classify` → clean **503** `{"detail":"live classifier unavailable…"}`; `/api/answer` → graceful (observed both a 200 abstention `no_verified_articles` and, when the run yields nothing, a 503) — **never 500**, health 200 throughout; live SSE → `heartbeat`(if >5s)/`answer`|`error`/`done`, server stays up.
- **Gotchas (for S10/S15):** (1) live latency ~9–15s/query; classify+answer **need a reachable LLM** (live LLM is unreachable from this box → the demo must lean on replay; S15 adds health-gating + nearest-precomputed fallback). (2) First live TF/CD dispatch lazy-loads the **74 MB KG (~26s)** — pre-warm or route TF/CD→replay. (3) **Env-flag concurrency:** E1/E5/E6/E7 are read **lazily at first dispatch** (run-time), after `_scoped_env` restores — so for non-default combos their live effect is best-effort under the single-user assumption; the construction-time flags (E4/HyDE, E2, E3) and all kwargs (recursion/adu/retry/ceiling/coverage/kg) are exact. (4) `AKN_NO_CITATION_GATE` only affects the freeform `RootController`/eval path, **not** the dispatched typed-handler path (like reranker/fusion-weights — honest no-op in the live path; keep the control but label it). (5) Live SSE is **post-hoc** (steps replayed after the blocking run, not mid-run). (6) `sse-starlette` MUST stay pinned `==2.1.3`. (7) Docker **not** re-rebuilt this session (disk at 97%, 4.8G free); only delta vs S3's working container is the `requirements.txt` pin (verified locally) — rebuild when disk allows.
- **Next:** S5 — Retrieval Lab endpoint. First step: `backend/app/services/retrieval_lab.py` using `akn_rlm` baselines (`baselines/{bm25,dense,hybrid,hybrid_rerank,kg}_pipeline`) + `retrievers/hybrid_fusion.rrf_fuse(weights=)` + `reranker.rerank()` → `POST /api/retrieval/compare` (offline; bm25/dense/hybrid/rerank need only the built indices). This is where per-channel BM25/Dense/HyDE candidates + RRF/rerank scores live (the §5 controls deferred from S4).

## S5 — Retrieval Lab endpoint (2026-06-03)

- **Status:** done
- **Shipped:** `backend/app/services/retrieval_lab.py`, `models/retrieval.py`, `routers/retrieval.py`, dependency/main/model exports, `backend/tests/test_retrieval_lab.py`; endpoint `POST /api/retrieval/compare`.
- **Contracts/paths:** request `{query|null,question_id|null,retrievers,k_each,top_k,rrf_weights:{bm25,dense},rerank_pool_size,doc_id,gold_article_ids|null}` with exactly one of query/question_id; response `{query,seeded_from_question,channels:[{name,params,candidates:[{rank,doc_id,article_ref,doc_title,snippet,score,is_gold}],n,elapsed_ms,offline_capable,note}],gold_article_ids}`. Channels: `bm25`,`dense`,`hybrid`,`hybrid_rerank`; `kg` accepted only as empty heavy/experimental note. `hybrid` mirrors `HybridRerankBaselinePipeline`: baseline hit→dict conversion, `rrf_fuse([bm25,dense], weights=[w_bm25,w_dense])`; `k` in `rrf_fuse` is `RRF_K=60`, not top-k. `hybrid_rerank` uses fused top `rerank_pool_size` then `akn_rlm.reranker.rerank(query,pool,k=top_k)`. Gold key rule is S2 shape `canonical_doc_id#eid`; candidates resolve through S1 `corpus.article(doc_id,article_ref)`. Reuses `PipelineService.bm25()/dense()`, S1 corpus, S2 benchmark seeding.
- **Verify:** `$PY -m pytest backend/tests -m "not live" -q` → `25 passed, 1 skipped, 3 deselected` (`models` skip = uncached cross-encoder). Host `cd backend && $PY -m uvicorn app.main:app --port 8000`: sample `{"query":"شروط الزواج","retrievers":["bm25","dense","hybrid"],"k_each":20,"top_k":10}` → bm25/dense/hybrid each `n=10` (`dense` first load ~7.6s). Weight reorder: bm25-heavy top5 `84-11#8, #7, #6, #9_bis, 75-58#11`; dense-heavy top5 `84-11#8, #4, #7, #9, #9_bis`. Seed `civ_ra_q01` sets `seeded_from_question` and gold ids; default top10 has no gold hits, but `k_each=500,top_k=100` flags BM25 gold `75-58_1975-09-26#art_1` at rank 67. `doc_id=84-11_1984-06-09` restrict returned only that doc in bm25/dense/hybrid. `kg` returns empty heavy note.
- **Gotchas:** offline = no LLM, but dense uses cached local `intfloat/multilingual-e5-small`; service preflights HF cache so missing encoder/reranker weights return empty noted channels instead of downloading. Reranker first successful call can be slow and needs cached `cross-encoder/mmarco-mMiniLMv2-L12-H384-v1`. KG is disabled in the Lab; use the S3 SQLite explorer. Weight range is non-negative; zero weight disables that list. Docker rebuild attempted and failed at image layer write (`triton/.../libcupti_static.a: no space left on device`) after pulling Torch/CUDA; pruned stopped build containers and recovered 11.47GB. Local verification passed.
- **Next:** S6 — Frontend foundation (layout, nav, theme, typed API client, RTL). Backend is now feature-complete for S6–S14; first step: `frontend/lib/api.ts` typed client covering all `/api/*` endpoints + shared components (`Nav`, `ArabicText`, `CitationChip`, `MetricCard`).

## S6 — Frontend foundation (2026-06-03)

- **Status:** done
- **Shipped:** Design tokens (`app/globals.css` HSL-channel vars: light/dark + `html[data-presenter]`), `tailwind.config.ts` (token colors incl. brand `gold`/`success`/`warning`/`info`, `tailwindcss-animate`, accordion/fade keyframes), `app/fonts.ts` (vendored woff2 in `frontend/fonts/` via `next/font/local` — Inter var + IBM Plex Sans Arabic 400–700), `components.json`. Data layer: `lib/{utils,format,types,api,sse}.ts`. shadcn set (hand-written, no CLI) `components/ui/`: button(+`gold`),card,badge(+gold/success/warning/info/muted),input,label,select,slider,switch,tabs,table,dialog,dropdown-menu,tooltip,accordion,scroll-area,separator,skeleton,sonner. `components/providers.tsx`. `components/shared/`: nav, health-badge, theme-toggle, presenter-toggle, presenter-provider, arabic-text, citation-chip, metric-card, page-shell, states, system-status. Re-skinned all 7 routes; `app/layout.tsx` (fonts+Providers+Nav+footer+presenter pre-paint script). `vitest.config.ts` + `lib/sse.test.ts`.
- **Contracts/paths:** Types in `lib/types.ts` mirror `backend/app/models/*` (friendly names: `Health,Meta,Article,DocumentSummary/Detail,SearchHit,QuestionSummary/Detail,Stats,Correctness,Metrics,Baselines,Classification,RunInfo,KGNode/Edge/Meta,Subgraph,NodeDetail,AnswerOptions,AnswerResponse,Citation,TrajectoryStep,PipelineConfig,ClassifyResponse,CompareRequest/Response,Channel,Candidate`). **Loose-by-design** (index sig / `Record<string,unknown>`): `Metrics,Baselines,PredictionView,QuestionDetail,ExpectedArticle` — don't tighten by guessing; concrete shapes come at S8/S9. `lib/api.ts`: `API_BASE=NEXT_PUBLIC_API_BASE`; central `apiFetch`→typed **`ApiError`** (`status===0`⇒`.isOffline`); fns `getHealth,getMeta,listDocuments,getDocument,getDocument{Xml,Text},getArticle,searchCorpus,kgMeta,kgSubgraph,kgNode,kgSearch,listQuestions,getQuestion,benchmarkStats,getMetrics,getBaselines,getRuns,getClassification,getPipelineConfig,resetPipeline,classify,answer,compareRetrieval` (all take optional `AbortSignal`). `lib/sse.ts`: **`streamAnswer(body,{onStep,onHeartbeat,onAnswer,onError,onDone,signal})`** — POST+ReadableStream (NOT `EventSource`); events `step|heartbeat|answer|error|done`; **`error` is terminal (no trailing `done`)**; pure `parseSSEFrame`/`createSSEParser` exported+tested. Theme: `next-themes` `class` (light/dark, persisted). **Presenter**: `data-presenter="true"` on `<html>` (scales root font `112.5%` ⇒ rem cascade + contrast); `usePresenter()` from `components/shared/presenter-provider`; pre-paint script + `suppressHydrationWarning` on `<html>` prevent FOUC. Tokens: shadcn `--accent` = neutral hover; brand accent = **`--gold`**; Tailwind colors use `/ <alpha-value>` (opacity modifiers work). Fonts via `font-sans`/`font-arabic`; use **`ArabicText`** for RTL; `.nums` = tabular figures. Shared props: `CitationChip{docId,articleRef,docTitle?,versionDate?,index?,kgNodeId?}`→`/corpus?doc=&article=`(+`/kg?node=`); `MetricCard{label,value,sublabel?,explanation?,delta?,accent}`; `ArabicText{as?,lines?}`; `PageContainer`/`PageHeader{eyebrow,title,description,actions}`/`ComingSoon{session,note}`; `states`: `EmptyState/ErrorState(error,onRetry)/OfflineBanner/LoadingState/MetricCardSkeleton/RowsSkeleton`. Providers order: QueryClient→Theme→Presenter→Tooltip→children+Toaster (defaults `staleTime 60s`,`retry 1`).
- **Verify:** `npm ls tailwindcss`⇒**3.4.19** (held through all installs). `npx tsc --noEmit` OK. `npm run build` OK — 7 routes, `.next/standalone/server.js` present. `npm test`⇒**10/10** SSE tests. Backend up (`$PY -m uvicorn app.main:app --port 8000`): `/api/health` has `corpus_ready:true`; `/api/meta`⇒`documents 45, articles 8868, benchmark_questions 244, kg_triples 765215` (what `SystemStatus` renders → health badge flips online, `getMeta` works). `/` prerenders Static with the client `SystemStatus` island. **SSE parser validated against REAL `sse-starlette` bytes** (captured `/api/answer/stream` replay of `civ_ra_q01` → `createSSEParser` yielded 9 `step`+1 `answer`+1 `done`, Arabic intact, `scores` present): confirmed framing is **CRLF** (`\r\n\r\n`), event names `step/answer/done`, no ping comments in replay. **NOT yet verified (browser-only, needs author `npm run dev`):** theme actually flipping, presenter actually scaling, Arabic glyphs rendering in Plex vs fallback, zero hydration warnings, and the visual polish itself.
- **Gotchas:** (1) Primitives **hand-written** (no shadcn CLI) to dodge the npm@11→Tailwind-v4 upgrade — if you ever run the CLI, don't let it rewrite `globals.css`/`tailwind.config.ts`. (2) Fonts **vendored** (`frontend/fonts/`, committed) — keep `next/font/local`, don't switch to `next/font/google` (build-network). (3) `lucide-react@1.17.0` (new major; uses `size-*` utils). (4) `**/*.test.ts` excluded from `tsconfig` ⇒ tsc/next-build ignore tests; vitest owns them. (5) `NEXT_PUBLIC_API_BASE` inlined at build = `localhost`, not `backend`. (6) `useQuery({queryKey:['health']})` is shared by Nav badge + home — same key dedupes. (7) Visual design is **hand-crafted directly** (the named design skills weren't invoked) — tokens read scholarly/restrained but the look is unverified by any tool; worth an author eyeball, and the design skills are best applied per-page at S7+ where there's real UI to critique.
- **Next:** S7 — Architecture page (offline, high-impact). First step: `lib/architecture.ts` content module (each node = `{id,label,what,inputs,outputs,code_path,metric}`) + `app/architecture/page.tsx` clickable nodes → detail panel; reuse `PageContainer/PageHeader`, `Card`, `Badge`, `CitationChip`.

## S6.5 — Visual overhaul: sidebar + palette + serif (2026-06-03)

- **Status:** done
- **Shipped:** Re-skin (no stack change) of the S6 foundation to "Ink & Orange", reproducing the Lovable reference the author liked. **New palette** in `app/globals.css` (token NAMES unchanged, only values; + new `--sidebar-*`). **Display serif** Instrument Serif (vendored woff2 `frontend/fonts/instrument-serif-latin.woff2`) via `next/font/local` as `fontDisplay`→`--font-display`; wired `font-display` family in `tailwind.config.ts` + `.font-display` utility. **App shell switched top-nav→left sidebar:** ported `components/ui/sidebar.tsx` (shadcn, adapted to v3) + deps `components/ui/sheet.tsx` + `hooks/use-mobile.ts`; new `components/shared/{app-sidebar,top-bar,aurora-backdrop}.tsx`; `app/layout.tsx` now `SidebarProvider`+`AppSidebar`+`SidebarInset`(TopBar+children+footer). `providers.tsx` `defaultTheme` light→**dark**. Restyled `shared/citation-chip` (mono pill, theme-aware `foreground/10` hairline) + `page-shell` (serif h1, brand eyebrow). Re-skinned `app/page.tsx` into the **"Ask Algerian law." hero** (gradient serif title, command bar FR·AR·EN + Top-k/Rerank chips + mic + orange-gradient send, suggestion chips, **static** answer-card + Top-Sources mock w/ RTL aligned excerpt). 6 placeholder routes inherit the new look via `page-shell`.
- **Palette→token (HSL channels):** Ink `#000814`=`216 100% 4%` (dark `--background`, `--primary-foreground`, `--gold-foreground`); Prussian `#14213d`=`221 51% 16%` (dark `--secondary`/`--popover`; card=`221 45% 12%` base vs popover raised); Orange `#fca311`=`37 98% 53%` (`--primary`,`--ring`, dark) / `37 96% 49%` (light); Gold `#ffd008`=`48 100% 52%` (`--gold`, dark); Alabaster `#e5e5e5`=`0 0% 90%` (`--secondary-foreground`), muted text `0 0% 72%`; White=`0 0% 98%` (dark `--foreground`). `--border` dark=`221 32% 22%` (lightened prussian, NOT white — `border-border` is full-alpha). New `--sidebar/-foreground/-primary(+fg)/-accent(+fg)/-border/-ring` (nested in tailwind `colors.sidebar`). **Gradient util** `.text-gradient-brand` / `.bg-gradient-brand` (orange→gold, replaces violet). `--radius` 0.6→0.75rem.
- **Contracts/paths:** `--font-display` → use `font-display` class (Tailwind) or `.font-display` util; **single-weight (400) — never pair with `font-bold/semibold`** (faux-bold). **Add a nav item:** push `{title,href,icon}` into `WORKSPACE`/`SYSTEM` in `components/shared/app-sidebar.tsx` (active highlight via `usePathname`, collapsed-icon tooltip auto). Sidebar collapses to icons via `SidebarTrigger` (in top-bar) or ⌘B; footer = `HealthBadge`+`PresenterToggle`. Use `<SidebarInset>` as the content `<main>`. **`lib/*` data layer UNTOUCHED** (`api/sse/types/utils/format`); shared-component props/exports **stable** (`CitationChip`/`MetricCard`/`ArabicText`/`PageContainer`/`PageHeader`/`ComingSoon`/states). **Orphaned (unused, left in place):** `components/shared/nav.tsx` + `system-status.tsx` (no importers; delete or reuse later).
- **Verify:** `npm ls tailwindcss`⇒**3.4.19**; `@tailwindcss/postcss`/`vite`/`tw-animate-css`⇒**none**. `npx tsc --noEmit` clean. `npm run build`⇒7 routes, `.next/standalone/server.js`+`.next/static` present. `npx vitest run`⇒**10/10**. `next start` → `/` 200 with hero (`text-gradient-brand`, "Ask Algerian law.", aligned RTL excerpt, `font-display`×7, sidebar "LexAlgeria"/"Workspace"), `/results` 200 serif title + ComingSoon. Compiled CSS has new tokens (`216 100% 4%`,`37 98% 53%`,`48 100% 52%`,`--sidebar:`), **0** old indigo/parchment/violet.
- **Gotchas:** (1) **Deliberate deviation:** prompt said add serif via `next/font/google`; used `next/font/local` (vendored) instead to honor CLAUDE.md hard-rule #4 + S6 gotcha (no build-network) — same `--font-display` result; author may veto. (2) **Design skill not invoked** (prompt said "if available, use it") — chose faithful reproduction since the reference IS the prescribed taste and palette/layout/fonts were fully specified; generative skills better at S7+ per-page. (3) Sidebar port: v4 `*-(--x)`→v3 `*-[var(--x)]`, `shadow-[…var(--sidebar-border)]`→`hsl(var(…))`; inner `TooltipProvider` removed (app-level one in `providers.tsx` is used). (4) `<html>` carries no `.dark` in SSR (next-themes adds pre-paint; `suppressHydrationWarning`) — default is dark via `defaultTheme="dark"`. (5) Home is now `'use client'` (controlled textarea; send/suggestions → `console.log` TODO for S10). (6) Harness blocks foreground `sleep` (exit 144) — not a build issue. **Browser-only, author to eyeball:** theme flip, presenter scale, Arabic glyphs, sidebar collapse animation, hydration warnings, overall polish.
- **Next:** S7 — Architecture page on the new design system. First step: `lib/architecture.ts` content module (`{id,label,what,inputs,outputs,code_path,metric}`) + `app/architecture/page.tsx` clickable nodes → detail panel; reuse `PageContainer/PageHeader`, `Card`, `Badge`, `CitationChip` (all serif-headed now).

## S7 — Architecture page (2026-06-04)

- **Status:** done
- **Shipped:** `lib/architecture.ts` — single source of truth: **20 ArchNodes** (4 data + 11 pipeline + 4 gates incl. corrective_retry + output) + **18 ArchEdges** + `ARCH_MODELS` (5, legend) + `HEADLINE_METRICS` (6) + `GROUP_META` + `formatMetric`/`getArchNode`. `app/architecture/page.tsx` (server: `PageHeader` + `<Suspense>`→client explorer). `components/architecture/`: `architecture-explorer.tsx` (state/URL/enrichment/headline strip/models+group legends/panel), `arch-diagram.tsx` (bespoke vertical-spine layout: data band → pre-dispatch spine → "Handler pipeline" Band → "Triple faithfulness gate" Band → output), `arch-node-card.tsx` (clickable `<button>`, per-id lucide icon, group accent, metric chip), `arch-detail-panel.tsx` (Sheet: what/inputs/outputs/copyable code paths/metric chip/related links), `arch-connector.tsx` (`FlowDown`/`FlowRight` gradient lines + animated pulse). `tailwind.config.ts`: added `flow-down`/`flow-right` keyframes+animations (additive). **Bespoke diagram — NO new dep** (no React Flow).
- **Contracts/paths:** `ArchNode{id,group,title,short,what,inputs[],outputs[],code[],metric?{label,value,href?,+overallKey?,kind?,digits?},links?[]}`; `ArchEdge{from,to,label?,kind:'flow'|'feed'|'loop'}`. **To add a node:** append to a `*_NODES` array in `lib/architecture.ts` (keep `code[]` REAL repo-root paths) + wire into `ARCH_EDGES`; diagram lays out by id-lists in `arch-diagram.tsx`. **Deep-link:** selection lives in URL `?node=<id>` via `useSearchParams`+`router.replace(...,{scroll:false})`; Sheet `onOpenChange(false)`→clears param; mount-read opens it (`?node=gate_faithfulness` ✓). **Offline-first:** all numbers are locked thesis values IN the module; `useEffect getMetrics()` reads `metrics.overall.<overallKey>` to enrich headline strip + node metric chips (shows "synced"/"live" badge), `.catch()` swallows offline — never blocks first paint. **All 7 code paths verified to exist** (note: `build_indices.py` lives at `akn_rlm/scripts/`, NOT under the package). Reuses S6.5 tokens/`font-display`/shared shell; no new palette.
- **Verify:** `npm ls tailwindcss`⇒**3.4.19**. `npx tsc --noEmit` clean. `npm run build`⇒**7 routes**, `/architecture` ○ prerendered (11.3 kB), `.next/standalone/server.js`+`.next/static` present. `npx vitest run`⇒**10/10**. `node .next/standalone/server.js` (BACKEND DOWN): `/architecture` **200**, `?node=gate_faithfulness` **200**, header SSR'd, no server errors. Diagram content (`Toulmin…`, `765,215`) bundled in `chunks/app/architecture/*.js` (renders offline from JS, no network).
- **Gotchas:** (1) Explorer calls `useSearchParams` ⇒ its subtree is **CSR** (Suspense fallback=null at prerender); only `PageHeader` is in SSR HTML — the diagram hydrates instantly from the bundled module (offline-OK), but initial HTML has no diagram nodes. (2) Animated pulse uses `animate-flow-down` + `motion-reduce:hidden` on the moving dot (static gradient line stays); respects `prefers-reduced-motion`. (3) Node icons mapped by id in `arch-node-card.tsx` (kept OUT of the content module to keep it pure-to-spec). (4) corrective_retry rendered linearly inside the gate Band (loop-back + direct gate→output "pass" edges live in `ARCH_EDGES`+node `what`, not drawn, to avoid clutter — the post-band connector is labelled "to output", NOT "pass", so the label can't contradict the layout). (5) **Browser-only, author to eyeball:** hover lift, Sheet slide, flow animation subtlety, keyboard focus ring, projector legibility, hydration warnings.
- **Next:** S8 — Results page. First step: `app/results/page.tsx` fetching `/api/results/metrics` + `/baselines` + `/classification`, rendering `MetricCard`s + Recharts (per-type bars, phase A→E progression, baseline + ablation tables, 8×8 confusion matrix).

## S7-FIX — Architecture redesign (2026-06-04)

- **Status:** done (presentation-only; no metric/content drift)
- **Shipped:** Reworked the Architecture page into a premium 2D **system map** after the S7 look was rejected ("boring / vibe-coded / gold≈orange / recursion invisible / too vertical"). Files: `components/architecture/arch-diagram.tsx` (full rewrite — serpentine lanes + measured-box SVG overlay), `arch-connector.tsx` (rewrite — `EdgeLayer` draws every connector from `ARCH_EDGES`), `arch-node-card.tsx` (double-bezel tile), `architecture-explorer.tsx` (machined headline tiles + phase/edge legend), `arch-detail-panel.tsx` (hairline polish), `lib/architecture.ts` → **only** `GROUP_META` (added `swatch`; retoned), and `tailwind.config.ts` (`flow-down`→`arch-dash` keyframe). Design skill `high-end-visual-design` invoked first (principles + mapping below).
- **Per rejection:** (1) **Horizontal space** — `ArchDiagram` lays 4 lanes (data rail · ingress · handler-pipeline reversed · gates) as L→R/serpentine rows in double-bezel bands; gates flow L→R in series, corrective_retry branches below the NLI gate. Collapses to a vertical stack < lg. (2) **Recursion visible** — connectors are 100% derived from `ARCH_EDGES` against **measured node boxes** (refs + ResizeObserver + `document.fonts.ready`); `kind:'loop'` back-edges (corrective_retry→summarizer) render as pronounced curved SVG arcs, plus a decorative self-loop on `recursion` (presentational, not an edge-content change). (3) **Gold/orange** — warm family reserved for active/selected + the flow line; phase identity now comes from `info` (data/IO) · `success` (gates) · neutral foreground (processing) + labelled bands. (4) **Premium** — double-bezel nesting, inset highlights/hairline rings (no gray borders), eyebrow lane tags, custom `cubic-bezier(0.32,0.72,0,1)` easing, magnetic hover, serif headings.
- **Design principles applied (high-end-visual-design):** ① Double-Bezel nested enclosures → lane bands + node tiles + headline tiles (outer shell ring + recessed inner core w/ inset top-highlight). ② Earned depth, no harsh shadows/gray borders → `ring-foreground/[0.06]` hairlines + `shadow-[inset…]`, warm glow only on selected. ③ Custom spring easing, no linear/ease-in-out → all transitions use `ease-[cubic-bezier(0.32,0.72,0,1)]`. ④ Magnetic micro-interactions → card lifts `-translate-y-1`, icon nudges, `active:scale`. ⑤ Macro-rhythm + eyebrow tags → `space-y-14` lanes, `00/01/02/03` mono-numbered uppercase tracking tags. ⑥ GPU-safe motion → animates only `transform`/`opacity` + SVG `stroke-dashoffset`, gated by `motion-reduce:animate-none`. (Skill's font/icon bans intentionally NOT followed — the S6.5 Instrument-Serif/Inter/Lucide stack + Ink-Orange palette are author-accepted & locked.)
- **Verify:** `npm ls tailwindcss`⇒**3.4.19**; `npx tsc --noEmit` clean; `npm run build`⇒10 pages, `/architecture` 13.4 kB, standalone intact. **Visually verified via headless Chrome + CDP** (`npx next start` so `/_next/static` serves — NOT `node standalone/server.js`, which 404s chunks): at 1440/1760 the diagram renders 20 nodes + 23 SVG edge paths, full-width lanes, the recursion + corrective-retry loop arcs are visible, deep-link `?node=recursion` opens the panel (what/inputs/outputs/code/metric) with the selected node in the warm accent. Offline-first: first paint from `lib/architecture.ts`; `/api/results/metrics` enrichment stays a non-blocking "synced" badge.
- **Gotchas:** (1) **Standalone static gotcha bit my smoke tests** — `node .next/standalone/server.js` alone serves HTML but 404s `/_next/static/*` (no JS → nothing hydrates). Copy `.next/static`+`public` beside it (Dockerfile does) or use `next start`. (2) The 231px h-overflow at exactly 1440px is **pre-existing layout** (256px sidebar + page-shell `max-w-[1400px]`), present on every route, out of scope — the diagram itself fits `main` (1400). (3) Edges are measured client-side → they paint a frame after the cards (overlay fades in); offline-fine. (4) Reduced-motion: the flowing dash is the only continuous motion and is gated; hover micro-transitions remain. (5) `/tmp/*.mjs` are throwaway CDP screenshot scripts, nothing under `frontend/`.
- **Next:** S8 — Results page. First step: `app/results/page.tsx` fetching `/api/results/metrics` + `/baselines` + `/classification`, rendering `MetricCard`s + Recharts (per-type bars, phase A→E progression, baseline + ablation tables, 8×8 confusion matrix).

## S8 — Results page (2026-06-04)

- **Status:** done (frontend only; no backend/`akn_rlm` change). Design skill `high-end-visual-design` invoked first (principles+mapping below).
- **Shipped:** `app/results/page.tsx` (server: `PageHeader` → client `ResultsDashboard`). `components/results/`: `results-dashboard.tsx` (client orchestrator — `Promise.allSettled` of `getMetrics`+`getBaselines`+`getClassification` w/ `AbortController`; all-fail→`ErrorState(retry)`, partial→render what loaded; skeletons while loading), `section.tsx` (double-bezel band w/ numbered eyebrow + serif title), `metric-cards.tsx` (6 headline `MetricCard`s), `per-type-chart.tsx` (bespoke H-bars), `phase-chart.tsx` (bespoke SVG area+line+latency), `baseline-table.tsx` (`ImprovementFactors` hero tiles + `BaselineTables` tier1/tier2), `ablation-table.tsx`, `metric-explainer.tsx`, `classification-card.tsx` (accuracy hero + per-type P/R/F1), `confusion-matrix.tsx` (8×8 CSS-grid heatmap), `utils.ts` (`num/pickNum/pickObj/pickStr/goldTypedFromNotes`, `EASE`). **Charts = bespoke CSS/SVG — recharts NOT installed** (matches Architecture, dodges Tailwind-v4 install risk). `tailwind.config.ts`: **additive** `transitionTimingFunction.spring` (named token; see gotcha 1).
- **Contracts/paths (consumed, unchanged):** `GET /api/results/metrics`→`metrics.overall.<key>` + `metrics.by_query_type.<type>.{citation_f1,abstention_f1}` + `metrics.counts.by_query_type.<type>`. `GET /api/results/baselines`→`improvement_factors{vs_strongest_deterministic 2.9, …minimal_rag 1.74, …direct_llm 1.64}`, `tier1_direct_llm[8]` (last=AKN-RLM), `tier2_deterministic_rag[11]` (last=AKN-RLM (locked)), `phase_progression[6]`, `ablation[5]`, `metric_definitions{9}`, `notes` (string — gold-typed 0.313 **parsed** from it, never literal). `GET /api/results/classification`→`{accuracy,n,per_type,confusion_matrix,labels[8]}`. **Matrix MUST iterate `labels[]` for both axes** (`unanswerable` is idx 2 in `labels` but last in the raw `confusion_matrix` JSON keys) + coerce `cm[t]?.[p] ?? 0`. **No metric value hardcoded** — page header copy is the only static text. `getRuns()` run-picker skipped (optional).
- **Design principles applied (high-end-visual-design):** ① Double-Bezel enclosures → `section.tsx` bands + `MetricCard`/factor/accuracy tiles (outer ring + recessed inner core w/ inset top-highlight). ② Earned depth, no gray borders/harsh shadows → `ring-foreground/[0.06]` hairlines + `shadow-[inset…]`. ③ Custom spring easing, never linear/ease-in-out → `ease-spring` token on bar/cell transitions. ④ Magnetic micro-interactions → confusion cells `hover:scale-[1.08]`, table-row tints, bar width-grow on mount. ⑤ Macro-rhythm + numbered eyebrow tags → `space-y-14` sections, `00`-style mono index tags. ⑥ GPU-safe + reduced-motion → animates only width/opacity/transform/`stroke-dashoffset`, `motion-reduce:` gated; SVG entrance via state-flip on mount. (Skill's font/icon/palette bans intentionally NOT followed — S6.5 Instrument-Serif/Inter/Lucide + Ink-Orange stack is author-locked, same call as S7-FIX.)
- **Verify:** `npx tsc --noEmit` clean; `npm run build`⇒**10 routes**, `/results` 11.6 kB, standalone intact; `npm ls tailwindcss`⇒**3.4.19** (no recharts, no v4 drift). **Headless render confirmed** (backend on :8000, standalone on :3210, `google-chrome-stable --headless --virtual-time-budget --dump-dom`): headline **0.3045** (4dp) + **gold-typed 0.313** + HCR/JIR **0.000** + abst **0.703** + AMF **0.471**; factors **2.9× / 1.74× / 1.64×**; AKN-RLM `ours` badge ×2; classifier **69.7%**; confusion = exactly **64** `True→Pred:N` cells, diagonal lines up (RA→RA:50). **Backend-down proven**: the CORS-blocked render (status-0 `ApiError`) showed the clean "Backend offline" `ErrorState`, no crash.
- **Gotchas:** (1) **`ease-[cubic-bezier(0.32,0.72,0,1)]` is silently DROPPED** — Tailwind flags it ambiguous + emits no CSS (Architecture has the same latent bug; its hover falls back to browser-default `ease`). Fixed here via named `ease-spring` token (`cubic-bezier(.32,.72,0,1)` now in built CSS). Build still warns from arch's literals (pre-existing, out of scope). (2) Citation-F1 card uses **`fmtScore(…,4)`** for `0.3045`; default 3dp gives `0.304`. The baseline-table AKN-RLM row showing `0.305` is the **curated** baselines.json value (≠ metrics.overall 0.30447) — both faithful, not a bug. (3) **CORS** allows only `:3000` (`backend/app/settings.py CORS_ORIGINS`); to headless-verify on another port, restart backend with `CORS_ORIGINS=…,http://localhost:3210`. (4) `node .next/standalone/server.js` needs `.next/static`+`public` copied beside it (S7-FIX gotcha 1); `next start` conflicts w/ a dev server sharing `.next/` (vendor-chunk 500s) — use a clean port + standalone. (5) Dashboard is CSR (client fetch) → SSR HTML is `PageHeader`+skeleton only; data paints after hydration (offline-first, expected). **Browser-only, author to eyeball:** light-mode contrast of `text-background` on the brightest diagonal cells, presenter scale, theme flip, bar entrance subtlety.
- **Next:** S9 — Benchmark page: `app/benchmark/page.tsx` consuming `GET /api/benchmark/questions` + `/{id}` + `/stats`; filter/search table w/ per-q hit badge, detail w/ gold article TEXT + precomputed prediction diff.

## S10a Main visualizer — shell + replay (2026-06-05)

- **Status:** done (frontend only; no backend/`akn_rlm` change). Design skill `high-end-visual-design` invoked first (principles + mapping below). Replaces the S6.5 static `app/page.tsx` mock with the fully-offline REPLAY experience. Live mode / control panel / history / Retrieval Lab deliberately NOT built — clean reserved slots left.
- **Shipped:** `lib/use-pipeline-stream.ts` (2 hooks + 1 util) · `components/pipeline/{utils.ts,step-card.tsx,trajectory-timeline.tsx,answer-panel.tsx,telemetry.tsx,benchmark-picker.tsx}` · rewrote `app/page.tsx` (orchestrator). **Consumes:** `streamAnswer({mode:'replay',question_id})` (SSE, lib/sse) · `listQuestions` (TanStack Query picker, `page_size=8` + pagination + debounced search + type filter) · `getHealth` (reused `HealthBadge` pill). No new deps; Tailwind held **3.4.19**.
- **`use-pipeline-stream` contract:** `usePipelineStream()` → `{status:'idle'|'running'|'done'|'error', steps:StepEvent[], answer:AnswerResponse|null, scores:ReplayScores|null, error, mode, elapsedS, runId, run(req:StreamRequest), reset(), cancel()}`. One AbortController; superseded runs ignored via a monotonic `runCounter` nonce; aborts on re-run + unmount. **S10b-ready:** `run()` takes a full `StreamRequest`, so `run({mode:'live',query,options})` needs no rewrite — heartbeat→`elapsedS` already wired; `error` treated as terminal (no trailing `done`). `useTrajectoryPlayback({runId,total,finished,reducedMotion})` → client cursor (play/pause/stepForward/restart/jumpToEnd, `activeIndex`, `isComplete`); **resets off `runId` (per-run nonce), NOT questionId** so replaying the same Q restarts; auto-stops + flips `playing` off at the true end; reduced-motion jumps cursor→total (instant). `usePrefersReducedMotion()` is SSR-safe (init `false`, set in effect). **Answer reveals on playback `isComplete`, not network `done`** (answer never pops in mid-walk).
- **Trajectory rendering:** continuous vertical subway spine; stations light up as the cursor walks (past=phase-accent solid · active=primary pulse-ring+glow · future=hollow/dim); connectors fade lit via `ease-spring`. Contiguous `recurse`-phase steps grouped (`groupRows`) into a primary-tinted **"Recursive descent" bracket** — staircase indent by `depth`, per-step `DepthMeter` (D{n} + filled ticks), lit `D1 D2 D3` axis → recursion depth unmistakable. Step token→English label/phase/icon via `stepMeta` (unknown steps degrade to humanised label + `other` phase); Arabic `summary` rendered RTL (`ArabicText`); `detail` payload as a definition grid (bool chips, %-for-confidence, Arabic-RTL strings, doc-id chips, `gap_decision` English gloss). All motion `transform`/`opacity`/colour, `motion-reduce:` gated.
- **Deferred slots (intentional, not empty boxes):** left rail = `BenchmarkPicker` (active) + dashed **`ReservedHistoryRail`** badged **S10c** (ghost rows); main top = dashed **`ReservedControlPanel`** badged **S10b** (ghost: root model·classifier·sub-LM·recursion·HyDE·KG); hero **Mode toggle** = Replay active + disabled `Live` (tooltip "arrives in S10b"). Answer/telemetry render in the right column + below.
- **Design principles applied (high-end-visual-design):** ① Double-bezel enclosures → timeline/answer/telemetry bands (outer ring + recessed core + inset top-highlight). ② Earned depth, no gray borders → `ring-foreground/[0.06]` hairlines + inset shadows; warm glow only on active. ③ Custom spring easing, never linear — **named `ease-spring` token** (NOT `ease-[cubic-bezier…]`, which Tailwind drops; S8 gotcha 1). ④ Magnetic micro-interactions → control `active:scale`, picker row `-translate-y-px`. ⑤ Macro-rhythm + numbered eyebrow tags (`00/01/02`). ⑥ GPU-safe + reduced-motion gated. (Font/icon/palette bans NOT followed — Instrument-Serif/Inter/Plex-Arabic/Lucide + Ink-Orange is author-locked, same call as S7-FIX/S8.)
- **Verify:** `npx tsc --noEmit` clean · `npm run build` → 10 routes, `/` 23.4 kB prerendered, standalone intact, lint clean, `npm ls tailwindcss`⇒**3.4.19** (the lone `ease-[cubic-bezier…]` build warning is **pre-existing** from `components/architecture/*`, not S10a). **Headless Chrome (CDP, backend up on :8000, NO LLM):** picked `civ_ra_q01` (row 1) → 9-station trajectory walks, **Recursive-descent bracket D1→D3 staircase visible**, active-station glow + dim future, RTL Arabic answer + 2 citations w/ Toulmin + `/corpus` deep-link chips, telemetry **1m18s / 30 sub-calls / depth 3** + all **5 faithfulness scores**. **Controls exercised (CDP, corrected reader):** Pause freezes the walk (cursor 1→1 over 1.7s), Step advances by exactly 1 (1→2→3), Restart rewinds to 0 and resumes to completion; reduced-motion (`Emulation.setEmulatedMedia reduce`) → instant reveal (answer < 2s, no 6s walk). Abstention (`acor_un_q01` via search "plea") → "The system abstained · Infected Jurisdiction", handler_summary fallback step. **Backend down (CDP blocked :8000):** picker `ErrorState` "Backend offline / Retry" + offline health pill, no crash. **Light + Presenter legible:** light mode renders the recursion bracket/hairlines with definition on white; presenter (`data-presenter`, root 16→18px) has **no horizontal overflow** (scrollWidth==clientWidth 1480). Screenshots `/tmp/s10a-shots/*.png`.
- **Gotchas:** (1) Replay citations carry `kg_source:null` + no node URI → chips deep-link **/corpus only**; `/kg` link omitted (CitationChip renders it only when `kgNodeId` passed — don't hand-reconstruct URIs). (2) Replay `latency_s` is the **locked-run** wall-clock (e.g. 77.9s) — labelled "locked run", not live. (3) Faithfulness scores shown **raw, never colour-coded by magnitude** (hcr/jir lower-better & 0 = best; `answer_faithfulness` reads 0.0 next to a good answer; `jir` only meaningful on unanswerable) — direction shown as "↓/↑ better" text + tooltips. (4) `next start` here 404'd `/_next/static` because a stale static-less standalone server held :3000 — to headless-verify, copy `.next/static`+`public` beside `.next/standalone/server.js` (Docker does this) — S7-FIX gotcha 1. (5) Replay step `summary` for `rank`/`supervisor` == the raw token (only some steps get an Arabic sentence) → step-card hides the summary line when `summary===step`. (6) `/tmp/s10a-*` are throwaway CDP scripts/shots, nothing under `frontend/`.
- **Next:** S10-BE — backend: additively expose `root_model` + `classifier_model` + a curated `sub_model` list as live `AnswerOptions` behind a flag (no metric drift) + a models catalog on `/pipeline/config`. **First step:** trace where `ROOT_LLM_MODEL` and the classifier model are consumed in `akn_rlm/rlm/dispatcher.py` (`build_dispatcher`) + `rlm/classifier.py`, and decide whether they can be threaded as `build_dispatcher(...)` kwargs vs env (mirror the S4 `_scoped_env` pattern in `services/pipeline.py:get_dispatcher`).

## S10b Main visualizer — conversational re-architecture (2026-06-04)

- **Status:** done (frontend only; **no backend / `akn_rlm` / dep change** — reused the S10a SSE hook, replay, trajectory verbatim). Pure layout/UX rework: rebuilt `app/page.tsx` into a ChatGPT/Claude conversational shell answering all 4 S10a rejections. Design skill `high-end-visual-design` re-read (structure/spacing/motion only; author-locked Instrument-Serif/Inter/Plex-Arabic/Lucide + Ink-Orange kept — same call as S10a).
- **Shipped:** NEW `components/pipeline/{composer.tsx, run-config.tsx, reasoning-trace.tsx}` · rewrote `app/page.tsx`. Deleted the S10a `ReservedControlPanel`/`ReservedHistoryRail`/`ModeToggle` blocks. **Consumes (all from API, nothing hardcoded):** `getPipelineConfig()` (run-config catalog), `resetPipeline()` (Reset cache + sonner toast), `streamAnswer` via existing `use-pipeline-stream` (replay), `listQuestions` (picker), `getHealth` (pill). No new npm dep (advisor steered me off `@radix-ui/react-popover` → reused `Sheet`+`Accordion`).
- **Empty → active layout:** `active = stream.status!=='idle'`. **Empty** = centered hero (`AuroraBackdrop soft` + eyebrow + h1 + `Composer variant="hero"` centered in `min-h-[calc(100dvh-13rem)]`). **Active** = `[SessionsRail 252px | conversation max-w-3xl]`; turn = **UserTurn bubble (right, RTL-aware) → ReasoningTrace (collapsible) → AnswerPanel → Telemetry** (author moved the grounded-synthesis answer BELOW the reasoning trace — Claude extended-thinking order: trace first, then the answer it produced); composer `variant="docked"` is `sticky bottom-0 z-20` with a top-fade gradient mask, content scrolls under (`pb-44` clears it). `turnRef.scrollIntoView` on each `runId` (auto when reduced-motion). Empty↔active swap + dock ride in via `motion-safe:animate-fade-up motion-reduce:animate-none`.
- **Run-config merged into composer:** `<RunConfig>` lives on the composer toolbar = 2 inline static on/off previews (Recursion, HyDE — `Lock` + live-only tooltip) + a **"Run config"** pill → right **Sheet**. Sheet renders the full `/pipeline/config` catalog by `type` (bool→Switch, enum/int-w-allowed→faux-select, int/float/string→Input), **Basic** inline + **Advanced** in an `Accordion`. **All 19 options are `requires_live:true`**, so the whole catalog renders **disabled** behind a prominent "applies to live runs — arriving soon" banner (intentional, not broken). **Reset cache** action (enabled, AbortController + toast). `const LIVE_ENABLED=false` is the single flip-point for S10-BE.
- **History-rail / sidebar decision (for S10d):** kept the **global `AppSidebar` (layout.tsx) as product nav**; conversation history is page-local, so it gets an in-page **`SessionsRail`** (lg-only, sticky, deliberate panel: disabled "New session", a live **Current** row = the active question, ghost rows, S10d badge) inside the conversation workspace — NOT another global sidebar. S10d wires real multi-session state here.
- **Verify (offline):** `npx tsc --noEmit` → exit 0 · `npm run build` → 10 routes, `/` **30.2 kB** prerendered, success · `npm ls tailwindcss` ⇒ **3.4.19**. No ESLint config exists in repo (build's lint step is a graceful no-op — that's how S10a "lint clean" was met; don't add one). Only build warning = pre-existing `ease-[cubic-bezier…]` from `components/architecture/*` (my files use only the `ease-spring` token). Answer panel now carries the **Answer · الإجابة / Sources · المصادر** bilingual rhythm (added to `answer-panel.tsx`) per the brief — addresses the "random verbosity" rejection together with the move to a 768px primary slot.
- **Browser-verified (headless Chrome, CDP, NO LLM):** the user's `:3000` `next dev` was serving **unstyled/non-hydrating** HTML to headless Chrome (its `.next` was churned by concurrent builds), so I verified against a **fresh prod standalone snapshotted to `/tmp/s10b-app` on :3100** with Chrome `--disable-web-security` (backend CORS only allows `:3000`, so :3100 needs the browser flag; the user's :3000 + :8000 were left untouched and my :3100 server + headless Chrome were torn down after). **Confirmed:** (1) **empty state** — centered hero composer, run-config merged in (Replay/Live, `Recursion🔒`/`HyDE🔒` inline previews, Run config); (2) **Run config Sheet** — `/pipeline/config` catalog renders, live-only banner, **8 disabled basic controls + Lock icons**, Advanced accordion expands to enhancers (E1/E2/Ceiling-breakers/Sub-LM), Reset cache + Done; (3) **picker** dialog (244 Qs, search/filter, RTL rows); (4) **replay `civ_ra_q01`** → **answer renders ABOVE the docked composer** (geometry: answerTop 273 < composerTop 843; composer gapToViewportBottom **22px** → docked; page scrollable), with **Answer·الإجابة / Sources·المصادر·2** + verified citation chips (100%/85%) + references; (5) **reasoning trace** auto-collapsed to "Reasoned through 9 steps · recursion depth 3 · 30 sub-calls · 1m 18s", expands to the full subway timeline with the **D1→D2→D3 recursive-descent staircase** + play/step/restart controls. Screenshots `/tmp/s10b-shots/{01-empty,02-runconfig-sheet,03-picker,04-active,07-trace-open}.png` (throwaway). The `sticky bottom-0` dock works because the ancestor chain (SidebarProvider wrapper → `SidebarInset` `main flex-col` → layout `flex-1`) has **no `overflow` clip** (the lone `overflow-auto` is inside the left-nav `SidebarContent` subtree). ⚠ My `npm run build` wrote `.next` while the user's `next dev` was running — **the user may need to restart their `:3000` dev server**.
- **Gotchas:** (1) **No `@radix-ui/react-popover`** in deps — use `Sheet`/`Dialog`/`Accordion`/`dropdown-menu` for overlays. (2) Nested the S10a `TrajectoryTimeline` inside ReasoningTrace by **flattening its bezel** via `className="!bg-transparent p-0 ring-0"` (twMerge wins over its `ring-1`/`p-1.5`/`bg`); its inner `bg-card/60` + own "00 Reasoning trajectory" controls header still show when expanded (play/pause/step/restart preserved there) — dropped the duplicate `00` from the trace header. (3) ReasoningTrace open state = `useEffect(()=>setOpen(!isComplete),[isComplete])` → expands during the walk, auto-collapses **once** on completion, re-expands next run, manual toggles persist. (4) Docked composer is `sticky` (not fixed height) — at scroll-bottom the global `<footer>` shows below it; reads as docked, no overlap (`pb-44`). (5) Typed input is present but **Send is gated/disabled** (Enter is `preventDefault`'d) — replay (Try-an-example) is the only run path this session. (6) Bash tool cwd is already `frontend/`; shell is **zsh** so `$PIPESTATUS` is empty — use `$?` / `$pipestatus`.
- **Next:** **S10c — rich trajectory/answer content.** Replay step `detail` is EMPTY, so content must come from elsewhere: (a) render **ADU/Toulmin triplets** from `answer.citations[].argumentation` `{claim,ground,warrant,rebuttal,backing}` (AnswerPanel already has a `Argumentation` renderer — surface it better / into the trace), and (b) feed the trajectory's **retrieve** station with REAL candidates via `POST /api/retrieval/compare` seeded by `question_id` (`compareRetrieval` in `lib/api.ts`, `CompareResponse.channels[].candidates`). **First step:** in `trajectory-timeline.tsx`/`step-card.tsx`, branch the `retrieve`/`rank` step cards to fetch+show compare candidates, and the `synthesize`/citation steps to show the Toulmin block.

## S10c Reasoning-trace real content (2026-06-06)

- **Status:** done (frontend only; **no backend / `akn_rlm` / dep change**). Design skill `high-end-visual-design` re-read (structure/spacing/motion only; author-locked Instrument-Serif/Inter/Plex-Arabic/Lucide + Ink-Orange kept). Enrichment is **additive** — layered on top of the existing S10a DetailGrid.
- **Design principles applied (high-end-visual-design):** ① Double-bezel nested enclosures → each `ChannelCard` = outer ring shell + recessed `bg-card/60` core w/ inset top-highlight; rerank baseline in a dashed-shell drawer. ② Earned depth, no gray borders/harsh shadows → `ring-foreground/[0.06]` hairlines + `shadow-[inset…]`; warm/gold glow reserved for the live-retrieve card + gold-hit rows. ③ Custom spring easing, never linear/ease-in-out → **named `ease-spring` token only** (NOT `ease-[cubic-bezier…]`, which Tailwind drops; S8 gotcha). ④ Magnetic micro-interactions → score bars grow 0→% on mount (`transition-[width]`), row/snippet/baseline expanders, hover hairline shifts. ⑤ Macro-rhythm + numbered/eyebrow tags → `in`/`→` channel-group eyebrows, collapsed-card content teasers, bilingual captions (Argument mining · تنقيب الحجج). ⑥ GPU-safe + reduced-motion → animates transform/opacity/width only, all `motion-reduce:` gated. (Font/icon/palette bans intentionally NOT followed — author-locked stack, same call as S7-FIX/S8/S10a/b.)
- **⚠ Brief was wrong on two verified points** (built on ground truth, not the brief): (1) replay `trajectory[].detail` is **NOT empty** — it is rich (`recursion`→gap_decision/sub_question/new_citations/top_confidence_pre+post; `rank`→kept/verified; `route`→routed_doc_ids; `faithfulness_gate`→fired/pre_passed/post_passed); the S10a grid already renders it. The real gaps were only the **articles** + **Toulmin** + **verify list**. (2) `supervisor_score` exists in predictions citations but the backend `CitationModel`/`_to_citation` **drop it** → "don't touch backend" honored → verify shows **verifier `confidence` + `verifier_relevant`**, labelled "verifier confidence" (NOT relabelled supervisor score). See [[project-trajectory-content-sources]].
- **Shipped:** NEW `components/pipeline/{retrieval-channels.tsx, toulmin.tsx, trace-context.tsx}` · NEW `lib/use-retrieval-compare.ts` · edited `components/pipeline/{step-card.tsx, reasoning-trace.tsx, answer-panel.tsx}` + `app/page.tsx`.
- **Data wiring:** `useRetrievalCompare(question_id)` fires **ONE** `POST /api/retrieval/compare {question_id, top_k:25, k_each:40}` per replayed Q — module-level cache (no refetch on re-run/Q-toggle), AbortController, non-blocking, errors swallowed. `app/page.tsx` calls it only when `stream.mode==='replay'`; passes `compare`+`status` → `ReasoningTrace`, which provides `TraceEnrichmentProvider {answer, compare, compareStatus, retrievalHostIndex}` around the timeline. `StepCard` consumes via `useTraceEnrichment()` (inert default → never crashes without a provider). **Retrieval renders ONCE** on `retrievalHostIndex` (the `rank` step, else `candidate_pool`, else first retrieve-phase step) — civ_ra_q01 has only `rank`.
- **Stations:** retrieve(host)→`RetrievalChannels`: BM25+Dense input cards, **Hybrid (RRF) = the live retrieve** (badge "live retrieve" + per-row contributing `BM25 #x · Dense #y` so fusion is legible), **Hybrid+Rerank in a collapsed section badged "baseline — NOT in the live RLM path · see Retrieval Lab"**; gold rows = gold ring/tint/star; RTL Arabic snippet expands; within-channel normalized score bars; honest one-liner ("faithful offline re-run, not a stored log") + gold-absent note ("recovered downstream via recursion"). argue(`adu_extract`)→`ToulminBlock` (claim/ground/warrant/**backing**/rebuttal, RTL) per cited article. verify(`supervisor`)→citation list w/ verifier_relevant badge + verifier confidence. route→handler+predicted-type. summarize→"Jump to answer" (anchors `#grounded-answer` added in page.tsx). Collapsed cards show a **content teaser** ("BM25 · Dense · Hybrid RRF" / "Toulmin ADU ×2" / "2 verified · 2 cited"). `answer-panel` refactored to the shared `ToulminBlock` (backing was already present).
- **Contracts:** `CompareResponse {query, seeded_from_question, gold_article_ids:string[]|null, channels:[{name,params,candidates:[{rank,doc_id,article_ref,doc_title,snippet,score,is_gold}],n,elapsed_ms,offline_capable,note}]}`. Citation `{doc_id,article_ref,doc_title,supporting_span,text,confidence,version_date,kg_source,argumentation:{claim,ground,warrant,backing,rebuttal},verifier_relevant}` — **no supervisor_score over the wire**.
- **Verify (offline):** `npx tsc --noEmit`→0 · `npm run build`→10 routes, `/` **34.8 kB**, success · `npm ls tailwindcss`⇒**3.4.19** (no v4); my files use only `ease-spring` (lone build warning = pre-existing `ease-[cubic-bezier]` in `components/architecture/*`). **Live backend (:8011) `POST /retrieval/compare` validated:** `fam_ea_q01` gold @ BM25 #1,#2 / Dense #1,#2,#6,#7 / **Hybrid #1,#2,#3** (gold-highlight + fusion paths fire), `fam_lm_q01` Dense pulls gold→#1, `civ_ra_q01`/`crim_lm_q01` gold beyond top-25 → gold-absent note. `hybrid_rerank` returns **n=0 + "cross-encoder weights not cached" note** here → ChannelCard shows the note under the baseline label (graceful). Arabic snippets + Arabic doc_titles confirmed (DocTitle auto-RTL). **In-browser verified (headless Chrome/CDP, prod standalone on :3100 → user backend :8000, reduced-motion, screenshots `/tmp/s10c-shots/*.png`):** replayed `com_ra_q01` (full pipeline route·recursion×3·rank·supervisor·adu_extract·gate, 6 citations) → **retrieve station** shows BM25+Dense input cards with **gold-highlighted rows** (gold @ BM25#1/Dense#1/Hybrid#1), score bars, RTL Arabic snippets, "Show all 25", the **Hybrid (RRF) "what the live RLM uses"** card with contributing ranks, + the **Hybrid+Rerank baseline drawer** (degrades to its "weights not cached" note); **verify station** = surviving citations w/ green "relevant" + verifier confidence; **argue station** = **5 Toulmin ADU triplets, all 5 fields incl. backing**, bilingual labels, RTL; recursion D1→D3 staircase intact; answer + sources + telemetry render. (Light-mode/presenter left to author eyeball, as prior sessions.)
- **Gotchas:** (1) **supervisor_score not exposed** (above) — verify uses verifier confidence; to show the real supervisor score, S10-BE could add an additive pass-through field (no metric drift). (2) `hybrid_rerank` is empty offline unless `cross-encoder/mmarco-mMiniLMv2-L12-H384-v1` is HF-cached — the baseline section degrades to its note, never crashes. (3) first `dense` call lazy-loads e5 (~7–14s) → the one-shot fetch is non-blocking + cached, so the trace paints first. (4) gold for the demo's default Q (civ_ra_q01, picker row 1) is beyond top-25 → shows the honest recursion note; pick `fam_ea_q01` to showcase gold highlights. (5) no ESLint config in repo → build lint is a no-op (as S10a/b). (6) Dark-mode + reduced-motion + gold tint **browser-verified**; **still author-eyeball:** light-mode gold/hairline contrast + presenter scale. (7) ⚠ my earlier `npm run build` wrote `.next` while the user's `:3000 next dev` was running (S10b gotcha) — **the user may need to restart their :3000 dev server**; my browser test used an isolated `/tmp` standalone on :3100 and left :3000/:8000 untouched.
- **Next:** **S10d — chat history side panel** via `localStorage` + resolve the `SessionsRail` (page-local) vs global `AppSidebar` pattern noted in S10b. **First step:** define a `Session {id, questionId, label, mode, createdAt}` store hook persisting to `localStorage`, populate the `SessionsRail` in `app/page.tsx` (replace the ghost rows), and wire row-click → `stream.run({mode:'replay',question_id})` + restore `selected`.

## S10d Chat history side panel (2026-06-06)

- **Status:** done (frontend only; **no backend / `akn_rlm` / dep change** — reused S10a's `usePipelineStream`, replay SSE, trajectory, retrieval-compare verbatim). Design skill `high-end-visual-design` re-read first (structure/spacing/motion only; author-locked Instrument-Serif/Inter/Plex-Arabic/Lucide + Ink-Orange kept — same call as S7-FIX/S8/S10a/b/c).
- **Shipped:** NEW `lib/use-session-history.ts` (store hook) + `components/pipeline/sessions-rail.tsx` (`SessionsRail` desktop + `SessionsSheet` mobile drawer) · rewrote `app/page.tsx` (unified shell + wiring; deleted the S10b placeholder `SessionsRail` ghost-row fn). **`use-pipeline-stream.reset()` already existed (S10a) → no hook change needed** (the brief's "add reset() if missing" was already satisfied).
- **Contracts/paths:** `Session {id, mode:'replay'|'live', questionId?, query?, label, queryType?, handler?, createdAt, lastRunAt}` (mode/query are forward-design for live S10e — this session only creates `mode:'replay'`). Persisted under versioned key **`lexalgeria.sessions.v1`** as `{v:1,sessions:Session[]}`; collapse flag under **`lexalgeria.sessions.collapsed.v1`** (`'1'`/`'0'`). `useSessionHistory()` → `{sessions, activeId, hydrated, createOrTouch, select, remove, clear, rename, newSession}`. **De-dupe by `questionId`** (touch `lastRunAt` + move to top, not duplicate). **SSR/hydration-safe:** state starts empty (server + first client render match), `localStorage` read in a post-mount effect; a **ref mirror is the synchronous source of truth** for mutators (React updaters run lazily/twice under StrictMode → `createOrTouch` reads `ref.current` and returns the resolved id deterministically; `makeId()` is computed outside the updater). Corrupt/absent JSON → empty (filtered by `isSession`), never throws. **`activeId` is RUNTIME-only (NOT persisted)** → a fresh load shows the empty hero with the rail listing history but no row highlighted (clean "new chat" feel); the list itself persists.
- **Page wiring:** `onPick(q)` → `setSelected`+`createOrTouch({queryType:q.query_type, handler:q.dispatched_handler})`+`stream.run`; a **runId-keyed, ref-read** effect refines the active session's subtitle from `AnswerResponse.{query_type_predicted,handler_used}` on completion (no exhaustive-deps lint here → never trusts the closure). `onSelectSession(s)` reconstructs a minimal `{id,question,query_type}` from the stored row → `stream.run({mode:'replay',question_id})`. `newSession()` = `stream.reset()` (aborts in-flight via its AbortController) + clear active + `setSelected(null)` → empty hero. Delete-active or Clear-all also reset to hero. `selected` is now a minimal `ActiveQuestion` type (not full `QuestionSummary`) so a stored row restores with no fabricated fields.
- **Collapse / mobile:** desktop `SessionsRail` = sticky `aside` (lg+), width animates **252px↔3.25rem** via `transition-[width] … ease-spring` (`motion-reduce:transition-none`); collapsed = slim icon rail (expand toggle + gradient New-session + count). Rows: auto-dir label (`isArabic`→RTL+`font-arabic`, else LTR — mirrors the picker, NOT blanket `ArabicText`), subtitle (queryType · handler · relative time), active highlight (`aria-current`), hover-revealed per-row delete (`Trash2`), `Clear all` footer, dashed **empty state**. < lg → `SessionsSheet` (a `History` pill trigger → left `Sheet`; reuses `@radix-ui/react-dialog`-backed Sheet, **NOT react-popover** per S10b). The rail renders in BOTH hero and active states **only when history exists** (load-bearing — else reloaded sessions would be invisible); first visit (zero history) = pure centered hero.
- **Verify (offline):** `npx tsc --noEmit`→**0** · `npm run build`→**10/10 static pages**, `/` **37.2 kB** prerendered, success · `npm ls tailwindcss`⇒**3.4.19** (no v4); my 3 files contain **0** `ease-[cubic-bezier…]` (7× `ease-spring`); lone build warning = pre-existing `components/architecture/*`. **Browser E2E (isolated, headless Chrome via raw CDP, prod standalone on :3100 → fresh backend :8000 with `--disable-web-security` since CORS allows only :3000; both torn down after):** **14/14 checks PASS** incl. **no console errors / hydration warnings** — (1) replay Q1 → active rail row appears; (2) session in `localStorage` (`n=1 qid=civ_ra_q01`); (3) reload → hero + rail still lists it; (4) click row → restores (re-runs, active); (5) collapse → `collapsed.v1='1'`; (6) survives reload collapsed; (7) expand; (8) New session → empty hero, history kept; (9) 2nd session; (10) delete row → n=1; (11) Clear all → empty hero + rail removed. Screenshots `/tmp/s10d-shots/{A-active-rail,B-reload-persist,C-collapsed}.png` (throwaway).
- **Design principles applied (high-end-visual-design — structure/motion only):** ① Double-bezel nesting → rail shell ring + recessed `bg-card/50` core; rows = bordered inner tiles w/ inset top-highlight. ② Earned depth, no gray borders/harsh shadows → `ring/border-foreground/[0.06–0.12]` hairlines + `shadow-[inset…]`; warm `primary` glow reserved for the active row + New-session button. ③ Custom spring easing, never linear → **named `ease-spring` token only** (width + hover; Tailwind drops `ease-[cubic-bezier…]` — S8 gotcha). ④ Magnetic micro-interactions → row `hover:-translate-y-px`, button `active:scale`, New-session icon `group-hover:scale-105`. ⑤ Macro-rhythm + eyebrow tags → uppercase tracked "SESSIONS"/"You" labels, mono subtitles, count badge. ⑥ GPU-safe + reduced-motion gated → transform/opacity/width only, all `motion-reduce:` guarded. (Font/icon/palette bans NOT followed — author-locked stack.)
- **Gotchas:** (1) **`activeId` not persisted by design** — reloaded conversation is NOT auto-restored (would fire a network run on every load); the rail lists history un-highlighted, a row click restores. (2) Width-animating the collapse contradicts the skill's "never animate width" guard but the brief explicitly asks for it + it matches the existing `AppSidebar` idiom — kept, `motion-reduce:transition-none`. (3) The post-answer enrich effect updates subtitle to predicted type/handler (may differ slightly from the gold `query_type` shown at pick); both faithful. (4) Harness blocks foreground `sleep` (exit 144) AND `pkill -f` (exit 144, matched its own shell) — waited on servers via a Node `setTimeout` poller; teardown by PID. (5) `frontend/tsconfig.tsbuildinfo` shows as modified — it's the tsc incremental cache (generated artifact), not a source edit. (6) ⚠ my `npm run build` rewrote the shared `.next` — **if the user has a `:3000 next dev` running they may need to restart it** (S10b/c gotcha); my E2E used an isolated `/tmp` standalone + fresh :8000 and left the user's ports untouched. **Still author-eyeball:** light-mode + presenter rail contrast (browser test was dark-mode).
- **Next:** **S10-BE** — backend: additively expose `root_model` + `classifier_model` + a curated `sub_model` list as live `AnswerOptions` behind a flag (no metric drift) + a models catalog on `/pipeline/config`. **First step:** trace where `ROOT_LLM_MODEL` and the classifier model are consumed in `akn_rlm/rlm/dispatcher.py` (`build_dispatcher`) + `rlm/classifier.py`, and decide whether they thread as `build_dispatcher(...)` kwargs vs env (mirror the S4 `_scoped_env` pattern in `services/pipeline.py:get_dispatcher`).

## S10-BE Live model-swap backend (2026-06-06)

- **Status:** done (backend-only; no frontend / no `akn_rlm` edits / no git). Defaults remain locked: `AnswerOptions()` model fields are all `None`, and omitted kwargs keep `RLMDispatcher`/classifier/supervisor constants.
- **Step-0 finding:** the production Main-page path is `PipelineService.answer -> get_dispatcher -> build_dispatcher -> RLMDispatcher -> typed handlers`. `RLMDispatcher` explicitly bypasses the freeform `RootController`; grep found no `RootController(` construction in `dispatcher.py`. Therefore `ROOT_LLM_MODEL` is **not** a live root-controller knob in the dispatched path, so **no `root_model` option/catalog role was exposed**. Real dispatched model consumers are: classifier closure (`make_llm_classifier_fn(model=...)`), sub-LM generator (`sub_model` into all typed handlers), and citation supervisor (`supervisor_model` already accepted by `RLMDispatcher`). The gpt-oss gap-probe exists inside handler recursion defaults but has no dispatcher-level kwarg, so it was not exposed as a fake root knob.
- **Shipped:** `backend/app/models/answer.py` adds optional `classifier_model` and `supervisor_model` alongside existing `sub_model`; `PipelineConfig` now includes `models` and `model_overrides_enabled`. `backend/app/settings.py` adds `ALLOW_MODEL_OVERRIDE=true` and `MODEL_CATALOG_JSON` for config-editable catalog overrides. `backend/app/services/answer_runtime.py` adds an offline static catalog: `generator` (locked Qwen3), `classifier` (locked Gemma), `supervisor` (locked gpt-oss), plus `fixed` local Table 3.6 context (`multilingual-e5-small`, `mDeBERTa`) explicitly not live knobs. `backend/app/routers/answer.py` returns that catalog from `/api/pipeline/config`.
- **Threading:** `PipelineService.get_dispatcher()` applies model choices only as request-safe injection: classifier model into `make_llm_classifier_fn`, `sub_model`/`supervisor_model` into `build_dispatcher` kwargs. `_options_key(..., allow_model_override=...)` memoizes per actual model combo; when `ALLOW_MODEL_OVERRIDE=false`, model fields normalize to `None`. No model choice mutates `ROOT_LLM_MODEL`, `SUB_LLM_MODEL`, `FALLBACK_MODEL`, or any other process-global env; env mutation remains only the existing E-flags/gate build scope.
- **Verify:** `/home/maab/Documents/pfe/methodology/PFE_locally/.venv/bin/python -m pytest backend/tests/test_answer_api.py backend/tests/test_pipeline_config.py -m "not live" -q` -> `10 passed, 3 deselected`. `/home/maab/Documents/pfe/methodology/PFE_locally/.venv/bin/python -m pytest backend/tests -m "not live" -q` -> `28 passed, 1 skipped, 3 deselected`. Temporary `uvicorn` on `127.0.0.1:8022` + `curl -s /api/pipeline/config` showed `classifier_model`, `sub_model`, `supervisor_model`, `models`, and `model_overrides_enabled:true`.
- **Gotchas:** `/api/classify` remains the default classifier preview because its request model has no `AnswerOptions`; live answer/stream model swapping is on `AnswerOptions`. Unsupported/bad live model ids are expected to fail during LLM use and surface through existing `PipelineLiveError` -> 503 / SSE `error`; offline unit test covers the no-crash path. `root_model` is intentionally absent to avoid a viva-trap dropdown.
- **Next:** **S10e — frontend:** wire model dropdowns from `/api/pipeline/config.models` into the composer run-config, enable Live mode behind health gating / `LIVE_ENABLED`, pass `classifier_model`, `sub_model`, and `supervisor_model` in live `AnswerOptions`, and consume existing SSE live `error`/`answer` events.

## S10e Live mode + model dropdowns (2026-06-06)

- **Status:** done (frontend only; **no backend / `akn_rlm` / dep change**; no git). Design skill `high-end-visual-design` re-read first (structure/spacing/motion only; author-locked Instrument-Serif/Inter/Plex-Arabic/Lucide + Ink-Orange kept — same call as S7-FIX…S10d).
- **Shipped:** NEW `components/pipeline/live-progress.tsx` · rewrote `components/pipeline/{composer.tsx, run-config.tsx}` + `app/page.tsx` · edited `lib/types.ts`, `lib/use-session-history.ts`, `components/pipeline/{utils.ts, telemetry.tsx}`, `tailwind.config.ts` (additive `live-sweep` keyframe/animation).
- **It was NOT a single flip-point** (S10b's note was wrong): the composer blocked live in **3** places (`ModeToggle` hardcoded-disabled, textarea `preventDefault`, disabled Send) **plus** `LIVE_ENABLED` in run-config. All reworked; **mode + run-config `overrides` lifted to `app/page.tsx`** (survive the hero↔active swap), passed into `Composer`. Live submit → `stream.run({mode:'live', query, options})`; AbortController cancels in-flight on submit/mode-switch/new-session/unmount (via existing `stream.reset/cancel`).
- **role→AnswerOptions:** generator→`sub_model` · classifier→`classifier_model` · supervisor→`supervisor_model` (added the latter two to `AnswerOptions`/types). Dropdowns render from `config.models`; the default-marked option = **null (use locked default)**; non-default = its `id`. `fixed` group (e5 + mDeBERTa) shown read-only (labels, not ids). When `model_overrides_enabled:false` → dropdowns disabled at defaults. The 3 model keys are filtered OUT of the generic option list. `buildAnswerOptions(overrides)` (in `pipeline/utils.ts`) maps the flat catalog-keyed overrides (incl. dotted `enhancers.eN`) → nested `Partial<AnswerOptions>`, dropping nulls (empty == locked Phase E).
- **Health-gating rule (binary, default-closed):** `isLlmReachable(health)` (in `pipeline/utils.ts`) is true ONLY on affirmative `llm` (`ok/reachable/up/available/ready/online/live/connected/healthy`). Live toggle: enabled iff reachable; else a re-checkable locked "Live" (click → `health.refetch()`; if reachable switch, else toast). Background flip→bounce to Replay. Live error event → graceful `LiveUnavailable` card (Retry live / Replay an example); restoring a live session while unreachable → same card. Telemetry omits the replay-only `scores` block in live, surfaces `am_faithfulness_score` from the answer instead. Live runs create `mode:'live'` history sessions (`createOrTouch` now matches by explicit `id` first so the post-answer enrich updates, not duplicates).
- **Live progress-state approach:** `LiveProgress` shows during the live `running`-before-steps window (`stream.mode==='live' && !hasSteps`); replay keeps `StreamingState`. Orbital glyph (ping/pulse/spin) + heartbeat-driven elapsed clock (`stream.elapsedS` smoothed by a 100 ms local tick) + indeterminate `animate-live-sweep` bar + a decorative CLASSIFY→SYNTHESISE stage arc (honest "illustrative — exact per-step detail streams with the trajectory" caption; we don't fake a % or the live stage). All `motion-reduce:` gated; the local tick is skipped under reduced motion (clock then advances on 5 s heartbeats).
- **⚠ Consequence to flag:** backend `health.llm` is **hardcoded `"unchecked"`** (`backend/routers/health.py:20`; real probing was deferred to S15). With the correct binary gate this means **Live is gated OFF in every environment — dev AND the viva — until the backend actually reports a reachable `llm`.** Live is built + correct, but **un-demoable against the real backend today.** To demo live: S15 backend probe (or a one-line author override). The brief's verify (c) explicitly accepts non-reachability, so this does not block S10e.
- **Verify (offline static):** `npx tsc --noEmit`→**0** · `npm run build`→**10 routes**, `/` **41.1 kB**, standalone intact · `npm ls tailwindcss`⇒**3.4.19** (no v4) · my files contain **0** `ease-[cubic-bezier…]` (the lone build warning is pre-existing `components/architecture/*`); compiled CSS has `animate-live-sweep` + `ease-spring`. **Browser E2E (isolated, headless Chrome via raw CDP):** (A) **real backend :8000** (`llm:"unchecked"`) via prod standalone :3100 → **DISABLED scenario PASS**: Live locked + "Live LLM unavailable" note, run-config disabled with the **3 model dropdowns + fixed read-only stack + defaults marked**, **replay streams to completion** (locked scores block), replay session recorded, **0 console errors**. (B) **mock backend** (`llm:"ok"`, :8077) via a standalone rebuilt to that base on :3101 → **LIVE scenario PASS**: Live enabled, run-config **ACTIVE** (interactive switches + 3 enabled model selects + fixed read-only + "this live run" banner), typed Arabic query → **`LiveProgress` heartbeat state** → steps animate → **answer + live telemetry (AM-faithfulness tile, no `scores`)**, **`live` session in history**, **0 console errors**. A real live-LLM run was **not possible** (endpoint unreachable + health unchecked) → used the mock; brief-accepted fallback. Screenshots `/tmp/s10e-shots/*.png` (throwaway).
- **Gotchas:** (1) the hardcoded-`unchecked` gate-off above. (2) reduced-motion is **code-gated** (`motion-reduce:` everywhere + the `LiveProgress` tick guard) but browser-verified **dark-mode only** — author should eyeball reduced-motion + light + presenter. (3) ⚠ my builds (incl. a temp build pointing at the mock) **rewrote the shared `.next`**; I **restored the default `localhost:8000/api` base** with a final `npm run build`, but if the user has a `:3000 next dev` running they may need to restart it (S10b/c/d gotcha). (4) a **real backend was already running on :8000** during my test (left untouched); my mock bound :8077 instead. (5) shell is **zsh** + harness blocks foreground `sleep`/`pkill -f` (exit 144) — and `pgrep -f s10e-chrome` **self-matches** the command string (looks like respawning chrome; it isn't). Cleanup confirmed via the `[s]`-trick grep: 0 test/headless chrome left. (6) Radix `Select` can't take an empty/`null` item value → `query_type`/null mapped through a `__null__` sentinel.
- **Next:** **S10f — motion & polish pass.** Progress/generation polish (`LiveProgress`), presenter scale across the live states, light-mode gold/hairline contrast, reduced-motion + light browser pass, and the **advisor-flagged aggregate-metric-on-a-single-slice caveat**: add a visible "aggregate metric · per-question slice" note beside `answer_faithfulness 0.000` / AM-faithfulness so a good answer next to a 0 reads honestly. First step: a small `<MetricCaveat>` note in `telemetry.tsx` + the replay `Telemetry` scores header.

## S10f Motion & polish (2026-06-06)

- **Status:** done (frontend only; **no backend / `akn_rlm` / dep / git**). Polish/QA pass — **no new features**. Design skill `high-end-visual-design` re-read first (structure/spacing/motion only; author-locked Instrument-Serif/Inter/Plex-Arabic/Lucide + Ink-Orange kept — same call as S7-FIX…S10e). Used as a **consistency lens**: one bezel/hairline/shadow/easing language across ALL states + themes.
- **Principles applied:** ① Double-bezel must read in EVERY theme → light now lifts via a real drop-shadow (`shadow-card`) + a stronger hairline; dark keeps its inset-highlight — **every change `dark:`-guarded so dark is byte-identical**. ② Earned depth, no gray borders → light `ring-foreground/[0.06]→/[0.09]`, borders `/[0.06–0.08]→/[0.1–0.12]`. ③ One easing language → only the `ease-spring` token (**0** `ease-[cubic-bezier]` added). ④ Reduced-motion honesty → all ambient/entrance motion gated. ⑤ Macro-rhythm + an honest, legible caveat. ⑥ GPU-safe (transform/opacity/shadow only).
- **Shipped (5 areas):**
  - **Aggregate-metric caveat (required)** — `telemetry.tsx`: new `MetricCaveat` = always-visible caption ("Aggregate / per-claim metric — may read low on a single question") + `Info` Tooltip (fuller NLI definition) + `/results` `<Link>`. Rendered in BOTH the replay "locked run scores" block (where `answer_faithfulness`/HCR/JIR read 0.000) and the live "this live run" block.
  - **Light-mode contrast** — theme-aware `dark:` splits on every pipeline bezel (`telemetry, reasoning-trace, live-progress, answer-panel, trajectory-timeline inner, composer, page.tsx PendingAnswer/StreamingState/LiveUnavailable, retrieval-channels ChannelCard shell+core, sessions-rail, ScoreTile`): light = `bg-foreground/[0.04]` tray + `ring/border-foreground/[0.09–0.12]` + `shadow-card` core lift; dark = original `0.025`/`0.06`/inset (unchanged). Cores no longer vanish on white.
  - **Reduced-motion** — `globals.css`: `@media (prefers-reduced-motion: reduce){.aurora{animation:none}}` (the aurora drift was the one ungated ambient loop). `StreamingState` spinner → `motion-reduce:hidden` (matches `PendingAnswer`). 5 collapse chevrons (reasoning-trace, retrieval-channels ×2, answer-panel, step-card) `+motion-reduce:transition-none` so they snap with their already-gated collapse grids. `live-progress` was already correctly gated (unchanged).
  - **Focus-visible** — branded `focus-visible:ring-2 ring-ring ring-offset-2 ring-offset-background` added to the primary demo controls (trajectory playback `ControlButton`, composer Send, sessions `SessionRow`) + the caveat Info/link.
  - **Presenter** — no element changes needed; verified it scales without overflow.
- **Verify:** `npx tsc --noEmit`→**0** · `npm run build`→**10 routes**, `/` **41.5 kB**, standalone intact · `npm ls tailwindcss`⇒**3.4.19** (no v4); my files add **0** `ease-[cubic-bezier]` (lone build warning = pre-existing `components/architecture/*`). **Dark byte-identical by construction** (each light change `dark:`-guarded to restore the original literal). **Browser (isolated, headless Chrome via raw CDP, prod standalone :3100 → real backend :8000, `--disable-web-security`, `prefers-reduced-motion:reduce` emulated, `theme=light`):** EMPTY hero, REPLAY-COMPLETE (`civ_ra_q01` → answer + telemetry with the **caveat visible above the score tiles** + sessions rail), and PRESENTER all render with definition on white; presenter **scrollW==clientW (overflowX 0)**; dark sanity shot unchanged. Shots `/tmp/s10f-shots/*` (throwaway).
- **Browser-verified vs logic-verified:** browser = light + reduced-motion + presenter for empty / replay-complete / sessions-rail / **caveat** + a dark sanity shot. **Logic/analogy-verified (not shot this pass):** (1) **LIVE states** (live-progress / live telemetry caveat / live-unavailable) — backend `health.llm` is hardcoded `"unchecked"`, so Live is gated OFF (real probe = S15); their only S10f change is the shared bezel light treatment + the same `MetricCaveat`, both verified on the replay cards, and live-progress motion was already gated. (2) **Retrieval-channel gold-hit rows** in light — the tested Q's gold was beyond top-k and the trace was collapsed; gold rows are unchanged from S10c (dark-verified there) and carry on white via `border-gold/45` + Star + gold pill; the surrounding ChannelCard got the same verified bezel lift.
- **Gotchas:** (1) all light fixes are `dark:`-guarded → **dark is provably untouched** (diff the `dark:` value against the prior literal). (2) `trajectory-timeline` OUTER bezel left unchanged on purpose — reasoning-trace flattens it via `!bg-transparent p-0 ring-0`; splitting it into `dark:` variants would defeat that override and re-show a tray in dark. Only its inner core got the light shadow. (3) ⚠ my `npm run build` rewrote the shared `.next`; the user had a `next-server` on **:3000** — **they may need to restart it** (S10b/c/d/e gotcha). Browser test used an isolated `/tmp/s10f-app` standalone on :3100 + real :8000, both torn down; **:3000/:8000 left untouched**. (4) gold pills (`bg-gold/15 text-gold`) are warm-on-warm in light — left as-is (the 45%-alpha gold border is the load-bearing signal); author eyeball if desired. (5) author-eyeball remaining: live states in light/presenter (un-demoable till S15) + gold-hit rows in light.
- **Next:** **S11 — Retrieval Lab:** standalone UI for `POST /api/retrieval/compare` — retriever multiselect, RRF weight sliders, rerank toggle, k; side-by-side scored lists + gold highlight; the honest home for retriever/fusion/rerank choice, distinct from the live RLM path. **First step:** `app/retrieval-lab/page.tsx` + a hook wrapping `compareRetrieval` (`lib/api.ts`) with the full request controls, reusing the `RetrievalChannels`/`RankRow` rendering from `components/pipeline/retrieval-channels.tsx`.

## S9 Benchmark page (2026-06-06)

- **Status:** done (frontend only; **no backend / `akn_rlm` / dep / git**). Design skill `high-end-visual-design` invoked first (structure/spacing/motion only; author-locked Instrument-Serif/Inter/Plex-Arabic/Lucide + Ink-Orange kept — same call as S7-FIX…S10f). The jury's window into ALB v3.0 (244 Qs) + how AKN-RLM did on each, fully OFFLINE from the locked run.
- **Shipped:** `app/benchmark/page.tsx` (server: `PageHeader` → `<Suspense>` → client `BenchmarkExplorer`), `app/benchmark/[id]/page.tsx` (server → client `QuestionDetailView`). `components/benchmark/`: `utils.ts` (outcome logic + loose-dict coercers + facet config), `outcome-badge.tsx`, `bench-stats.tsx` (bespoke CSS bars, clickable→filter), `filters.tsx`, `question-list.tsx` (+ pagination), `benchmark-explorer.tsx` (orchestrator), `gold-vs-pred.tsx`, `trajectory-mini.tsx`, `question-detail.tsx`. **Additive edits:** `app/page.tsx` (`?replay=<id>` deep-link), `components/pipeline/telemetry.tsx` (`export MetricCaveat`). **Consumes:** `listQuestions` · `getQuestion` · `benchmarkStats` (TanStack Query, AbortController via `signal`, `placeholderData:keepPreviousData`); reuses `ResultsSection`, `MetricCard`, `MetricCaveat`, `CitationChip`, `ArabicText`, `ToulminBlock`/`hasToulmin`, `stepMeta`/`PHASE_META`/`gapDecisionLabel`/`isArabic`, shared `states`. No new deps; Tailwind held **3.4.19**; my files add **0** `ease-[cubic-bezier]` (lone build warning = pre-existing `components/architecture/*`).
- **URL = single source of truth:** `BenchmarkExplorer` reads filters/page from `useSearchParams`; the TanStack queryKey derives straight from it (shareable/restorable). Only the search box keeps a 300 ms debounced local mirror → `router.replace` into `?q=`. Facet change / stats-bar click / pagination all `updateParams` (filters reset page→1). `<Suspense>` keeps the route prerenderable; the Main page reads `window.location.search` (NOT `useSearchParams`) so it stays out of Suspense.
- **`?replay=<id>` deep-link (added to `/`, guarded):** one-shot `useEffect` (ref-guarded, empty deps, no-abort cleanup → StrictMode-safe) reads `window.location.search`; if `replay` present it strips the param (`history.replaceState`) then `await getQuestion(id)` → `setMode('replay')` + `createOrTouch` + `stream.run({mode:'replay',question_id})`. **Guard:** no param → early-return (untouched empty hero); invalid id / backend down → `getQuestion` throws → `catch` no-op → empty hero. Only ever drives replay; never touches live/history → **S10 not regressed** (browser-proven trio below). Detail's "Open in pipeline visualizer" = `<Link href="/?replay=<id>">`.
- **Outcome badge (`computeOutcome`, drives list + detail):** `!has_prediction`→none · `correctness.abstention_scored`→`predicted_abstain ? correct-abstain : miss` · else `f1≥0.999`→hit (P=R=1) · `n_correct>0`→partial · else miss. (A recall-1/precision<1 row = "partial"; legend states the rule via the badge tooltip.) **gold-vs-pred** computed from `prediction.gold_article_ids` vs `pred_article_ids` → matched∩ / missing(gold∖pred) / extra(pred∖gold) + P/R/F1 (`prediction.correctness`); for unanswerable it shows the **abstention verdict** (`predicted_abstain` vs `gold_abstain`, gold defaults `query_type==='unanswerable'`) instead of the article diff. **Answerability convention honoured:** abstention positive set = `query_type==='unanswerable'` (40); the `answerable` filter (false=67) is a separate facet, labelled so the two aren't conflated.
- **Design principles applied (high-end-visual-design; structure/spacing/motion only):** ① Double-bezel enclosures → reused `ResultsSection` bands (outer hairline shell + recessed core) for stats/list/detail sections + `MetricCard` tiles. ② Earned depth, no gray borders → `ring/border-foreground/[0.06–0.1]` hairlines, `shadow-card` on hover, toned `ring-inset` outcome pills. ③ One easing language → only the `ease-spring` token on every transition (bar-grow, row lift, chevron, diff) — **0** `ease-[cubic-bezier]`. ④ Magnetic micro-interactions → list-row `hover:-translate-y-px` + chevron nudge, stats bars grow on mount + brighten on hover. ⑤ Macro-rhythm + numbered eyebrow tags (`01/02/03/04`) + generous `space-y`. ⑥ GPU-safe + reduced-motion → animates only width/opacity/transform, `motion-reduce:` gated, debounced/abortable fetches. (Font/icon/palette bans NOT followed — author-locked stack, same call as S7-FIX…S10f.)
- **Contracts/paths (consumed, unchanged):** `GET /api/benchmark/questions` (params `query_type,difficulty,category,answerable(bool),language,split,q,page,page_size≤100`) → `{total,page,page_size,items[]}`; `QuestionSummary.correctness{n_gold,n_pred,n_correct,precision,recall,f1,abstention_scored}` drives the badge. `GET /…/{id}` → `expected_articles[].text` (GOLD full Arabic text, RTL), `reasoning_chain[]`, `ground_truth_answer`, `prediction{answer_text, predicted_citations[]{doc_id,article_ref,doc_title,supporting_span,confidence,argumentation(dict→Toulmin)}, trajectory[]{step,depth,+step-keys — NO summary}, hcr/jir/answer_faithfulness/citation_groundedness/am_faithfulness_score, latency_s,sub_call_count,retry_count, predicted_abstain,gold_abstain, gold_article_ids,pred_article_ids, correctness}`, `gold_vs_pred`. `GET /…/stats` → 6 count maps (each sums 244). **TrajectoryMini** synthesises a gloss per step (raw trajectory has no `summary`): `stepMeta` label + `D{depth}` + Arabic `sub_question` RTL + factual chips (bool ✓/✗, array-len, numbers, `gapDecisionLabel`).
- **Verify (offline):** `npx tsc --noEmit`→**0**; `npm run build`→**10 static routes + `/benchmark/[id]` ƒ**, `/benchmark` 9.27 kB, `/benchmark/[id]` 9.74 kB, `/` 30.3 kB, standalone intact, lint clean; `npm ls tailwindcss`⇒**3.4.19**. **API (curl, backend `KG_BUILD_ON_START=false`):** total **244**, multi_hop **26**, unanswerable **40**, answerable=false **67**, stats each sum 244; `civ_ra_q01`→2 expected_articles w/ text, 2 predicted_citations (argumentation=dict), 9 traj steps, gold_vs_pred n_gold2/n_pred2/n_correct1 → P/R/F1 0.5 ("partial"); `lab_un_q03`→abstention_scored, predicted=gold=abstain, f1 1.0 ("correct-abstain"). **Browser (headless Chrome `--dump-dom`, isolated standalone :3100 → backend :8000, CORS+`:3100`):** LIST = "244 questions total" + BenchStats 244 + 25 rows + "1 / 10"; `?query_type=multi_hop` = select restored to "Multi Hop" + "26 questions match the filters" + `*_mh_q*` rows (URL-restored filter); DETAIL `civ_ra_q01` = Target articles + Gold-vs-predicted (Matched/Missing/Extra) + Precomputed prediction + Toulmin + Reasoning chain + Run metrics + replay link. **Deep-link trio:** `/`→hero present (no auto-run); `/?replay=civ_ra_q01`→hero ABSENT + Document-routing/Reasoning-trajectory/Telemetry (replay fired, S10 intact); `/?replay=__garbage__`→hero present (clean fallback). **Backend down** → `/benchmark` shows "Backend offline"/"unreachable"/"Retry" + "Distribution unavailable", no crash.
- **Gotchas:** (1) ⚠ my `npm run build` **rewrote the shared `.next`** + copied `.next/static`+`public` into `.next/standalone/` for the :3100 test — if the user has a `next-server` on :3000 they may need to restart it (S10f gotcha). The :3100 standalone + the :8000 backend (started by me) were both **torn down**; nothing was listening on :3000/:8000 before, so they're left clean. (2) Verified via **DOM dumps** (not PNGs) to respect the OOM budget — author may eyeball light/presenter + Arabic RTL rendering live. (3) Backend for `/benchmark` doesn't need the 74 MB KG — start it with `KG_BUILD_ON_START=false` to avoid the slow/heavy KG build. (4) raw `trajectory[]` carries **no Arabic `summary`** (only `step`/`depth`/step-keys) — do NOT expect the SSE-replay step sentences here; TrajectoryMini glosses from the real keys. (5) predicted-citation scalars are clean-typed in the JSON (`confidence` float, `argumentation` dict) — coercers in `utils.ts` still defend against drift. (6) `/tmp/s9-shots` are throwaway dumps; nothing under `frontend/`.
- **Next:** **S11 — Retrieval Lab** (unchanged from S10f's pointer): standalone UI for `POST /api/retrieval/compare` — retriever multiselect, RRF weight sliders, rerank toggle, k; side-by-side scored lists + gold highlight; the honest home for retriever/fusion/rerank choice, distinct from the live RLM path. **First step:** `app/retrieval-lab/page.tsx` + a hook wrapping `compareRetrieval` (`lib/api.ts`), reusing `RetrievalChannels`/`RankRow` from `components/pipeline/retrieval-channels.tsx`.

## S11 Retrieval Lab (2026-06-07)

- **Status:** done (frontend only; **no backend / `akn_rlm` / dep / git**). Design skill `high-end-visual-design` invoked first — structure/spacing/motion only; author-locked Instrument-Serif/Inter/Plex-Arabic/Lucide + Ink-Orange-Gold kept (same call as S7-FIX…S10f). Standalone, fully-OFFLINE instrument for `POST /api/retrieval/compare`.
- **Shipped:** `app/retrieval-lab/page.tsx` (client orchestrator: state + honesty card + warming/empty/error states + reused channels). `components/retrieval-lab/`: `controls.tsx` (double-bezel `LabControlsPanel`: input-mode segmented, retriever chips, RRF weight sliders, rerank Switch + pool, k_each/top_k sliders, status pill + button-in-button Run + Reset), `seed-input.tsx` (`SeedInput` — reuses `BenchmarkPicker` in a Dialog; selected-question gold chip). **New hook** `lib/use-retrieval-lab.ts` (`useRetrievalLab` + `DEFAULT_CONTROLS` + `toRequest`) — parameterized, **debounced 400 ms**, built on `useQuery` (Abortable via `signal`, `placeholderData:(prev)=>prev` keeps prior channels visible while re-running so Hybrid visibly reorders, `retry:false`); `runNow()` flushes the debounce / retries. **DID NOT touch** `lib/use-retrieval-compare.ts` (S10c one-shot cached hook the Main page depends on) — new file instead. **Additive sidebar edit:** `components/shared/app-sidebar.tsx` → `{title:'Retrieval Lab',href:'/retrieval-lab',icon:SearchCheck}` in WORKSPACE (renders on every page). Reuses `RetrievalChannels`/`CompareStatus`, `BenchmarkPicker`, `ArabicText`, `isArabic`, `EmptyState`/`ErrorState`, `Skeleton`, `Slider`/`Switch`. No new deps; Tailwind held **3.4.19**; my files add **0** `ease-[cubic-bezier]` (lone build warning = pre-existing `components/architecture/*`).
- **Live-vs-baseline labelling (viva honesty, plan §5):** page `HonestyCard` states the live RLM retrieves with **Hybrid (RRF) of BM25+Dense at equal weights (1.0/1.0)** and that the fusion-weight sliders + the rerank baseline are **Lab-only, NOT in the live path** — plus a concise "how RRF fuses" line (`Σ wᵢ·1/(60+rankᵢ)`). The reused `RetrievalChannels` badges Hybrid "live retrieve" + Hybrid+Rerank "baseline"; since that badge is hardcoded, the **honesty caveat lives at page+controls level** (Fusion-weights group shows `live = 1.0/1.0` → flips to amber `exploring` when weights deviate; Re-ranking group carries a `baseline` chip + "not in the live RLM path").
- **Contract (consumed, unchanged):** `compareRetrieval(req,signal)`→POST `/api/retrieval/compare`. **EXACTLY ONE of `query`|`question_id`** — `toRequest()` enforces it (seed mode sends only `question_id`, query mode only a non-empty `query`); Run gated on `isValid` so we never 422. Seeding by `question_id` auto-resolves the query AND `gold_article_ids` → gold ★ rows. `CompareResponse{query,seeded_from_question,gold_article_ids,channels[]{name,params,candidates[{rank,doc_id,article_ref,doc_title,snippet,score,is_gold}],n,elapsed_ms,offline_capable,note}}`.
- **Verify (offline):** `npx tsc --noEmit`→**0**; `npm run build`→**11 routes** incl. `/retrieval-lab` (11.4 kB / 160 kB First-Load), standalone intact; `npm ls tailwindcss`⇒**3.4.19**; sidebar entry static → on every page. **Backend contract (temp backend `:8000` `KG_BUILD_ON_START=false`, torn down):** seed `fam_ea_q01`→`seeded_from` set + 3 `gold_article_ids`, Hybrid top-3 all `is_gold` ★; **weight nudge bm25 0.4/dense 2.0 REORDERS the visible top-6** (art_1↔art_2 swap + ranks 4–6 change) ✅; first dense call **8.7 s** then **0.1 s** warm (warming state earns its place); `hybrid_rerank`→`n=0` + note "cross-encoder weights not cached" (RetrievalChannels degrades to the collapsed baseline note); free-form `ما هي شروط الزواج؟`→bm25/dense/hybrid lists, no gold; both `query`+`question_id`→**422** (UI prevents by construction). Reduced-motion gated; RTL snippets via reused renderer. **Browser screenshot:** skipped (optional) to respect OOM budget — data path + build proven above.
- **Gotchas:** (1) **dense/hybrid lazy-load** the e5 encoder on the FIRST call (~8–14 s) → page renders a real warming state (`status==='loading' && !compare`, e5 note when a dense-family retriever is selected), then fast; debounced auto-run means a typed query fires ~400 ms after you stop typing (or ⌘/Ctrl+Enter / Run). (2) `hybrid_rerank` returns **n=0 + "weights not cached"** locally (cross-encoder not HF-cached) — graceful by design, never a crash. (3) RRF weight sliders are disabled (dimmed) unless Hybrid is selected; both-zero weights are allowed (degenerate but backend-safe). (4) ⚠ my `npm run build` **rewrote the shared `.next`** — if the user has a `next-server` on :3000 they may need to restart it (S9/S10f gotcha). Temp backend on :8000 was started by me and **torn down**; nothing was on :3000/:8000 before, left clean. (5) reused `RetrievalChannels` slices to top-6 with expand + its intro says "seeded by the question" (slightly loose for free queries) — left as-is to honour the no-reimplement mandate.
- **Next:** **S12 — AKN corpus explorer `/corpus`:** side-by-side text⟷AKN XML, eId tree nav with status badges, FRBR metadata, in-doc + cross-corpus search, per-doc summary; consumes `/corpus/documents[/{id}[/xml|text]]`, `/corpus/articles/{doc}/{ref}`, `/corpus/search`. **First step:** `app/corpus/page.tsx` + a documents-list hook over `listDocuments` (`lib/api.ts`) and a doc-detail view wiring `getDocument`/`getDocumentXml`/`getDocumentText`.

## S12 Corpus explorer (2026-06-07)

- **Status:** done (frontend only; **no backend / `akn_rlm` / dep / git**). Design skill `high-end-visual-design` invoked first — structure/spacing/motion only; author-locked Instrument-Serif/Inter/Plex-Arabic/Lucide + Ink-Orange-Gold kept (same call as S7-FIX…S11). The jury's reading instrument for the 45 official AKN documents (~8,868 articles): RTL Arabic ⟷ raw AKN XML, eId tree, FRBR metadata, search — fully OFFLINE. **Citation deep-link target** (`/corpus?doc=&article=`) consumed correctly.
- **Shipped:** `app/corpus/page.tsx` (server: `PageHeader` → `<Suspense>` → client `CorpusExplorer`). `components/corpus/`: `utils.ts` (**pure, load-bearing**: `nodeIdForArticle`/`buildArticleIndex`/`resolveRefToEid`/`nodePathForArticle`/`structureDepth`/`levelCounts`/`estimateArticleHeight`/`statusMeta`/`docTypeMeta`/format+level config), `scroll.ts` (`settleScroll` rAF loop + `prefersReducedMotion`), `xml-highlight.ts` (pure `highlightXmlToHtml` for the `<pre>` fallback), `format-chips.tsx` (`FormatChips`+`DocTypeBadge`), `corpus-search.tsx` (`useCorpusSearch` debounced+abortable, `CorpusSearchBox`, `SearchResultsRegion` w/ RTL `<mark>` highlight), `document-library.tsx` (`DocumentLibrary`+`LibraryState`+`DocumentCard`), `structure-tree.tsx`, `text-pane.tsx`, `xml-pane.tsx`, `metadata-panel.tsx`, `document-reader.tsx` (orchestrator), `corpus-explorer.tsx` (URL shell). **Consumes:** `listDocuments`·`getDocument`·`getDocumentXml`·`getArticle`·`searchCorpus` (TanStack Query, Abortable via `signal`; XML `enabled` only when its pane is visible). Reuses `ArabicText`, shared `states`, `PageHeader`, `Sheet`, `Select`, `Skeleton`. No new deps; Tailwind held **3.4.19**; my files add **0** `ease-[cubic-bezier]` (lone build warning = pre-existing `components/architecture/*`).
- **Selection key = `eid` (NOT `article_ref`):** verified against live S1 — `article_ref` is the BARE number ("1") and is **non-unique** within a doc (Civil Code has two "art 1": eid `art_1` and eid `art_الاولى`); `eid` is the unique anchor. One `selectedEid` drives tree (`aria-current`) + text pane (accent bg + rail) + XML pane + URL.
- **Tree mapping (reproduces backend `_hierarchy` EXACTLY):** `nodeIdForArticle(docId,ancestors)` = `f"{parent}/{level}:{value}"` over `[book,part,title,chapter,section,subsection]`; eids grouped by node.id. **Verified 0 mismatches** vs the API's `node.article_refs` counts on Civil Code (1055), Penal (660), Constitution (78). Flat docs (constitution: 0 structural children) map all articles to root (id===doc_id) and render under root — no empty tree.
- **text⟷XML sync (DOMParser primary, `<pre>` fallback):** XML pane parses `application/xml`; **structured tag-coloured tree** with `data-eid` anchors for the ~42 small/medium docs; for the **3 biggest laws** (or a parse error / element count > 3500) it switches to a single highlighted `<pre dangerouslySetInnerHTML>` ("Large document — fast highlighted view") — far cheaper than mounting ~6k React nodes. Selecting an article scrolls BOTH panes to its eid (tree mode → React ref map; pre mode → `[data-eid]` DOM query, **no CSS-escaping** of Arabic eids) + a transient flash. eIds read via `getAttribute('eId')`/`[data-eid]`, **never tag-name selectors** (AKN default namespace breaks `getElementsByTagName`).
- **Deep-link (`?doc=&article=`, the make-or-break):** `CorpusExplorer` reads `?doc`/`?article` from `useSearchParams`; `?doc` opens `DocumentReader` (keyed on docId → per-doc state resets). Bare `?article` ref → `resolveRefToEid` (exact eid → `art_<ref>` → first-by-ref in doc order, matching backend `setdefault`); unresolved/normalised-Arabic refs fall back to `getArticle`. Adopt-effect depends **only on the resolved targetEid** so local clicks (which set `selectedEid` + write the unique **eid** to `?article`) never get clobbered. Invalid doc → ErrorState + back; invalid article → opens at top. Library filters are **local state** (preserved across open/close), not in the URL (not part of the shareable contract).
- **Big-doc perf:** every article block carries `content-visibility:auto` + `contain-intrinsic-size` (from `estimateArticleHeight`) → all 1055 stay in the DOM (refs/scroll always resolve) while offscreen paint is skipped. `settleScroll` re-centres across ≤4 rAFs until the target's viewport-top stabilises (kills content-visibility scroll drift on far jumps); near/visible targets get one smooth scroll. Containers (not 1000 blocks) animate; reduced-motion gated throughout.
- **Honest degrades:** `status` is hardcoded `null` for every article in S1 → tree/text render the badge *capability* (lights up if status ever lands) but the metadata panel says "Per-article status is not recorded in this corpus" rather than draw an empty chart (project honesty rule). **Known limitation (backend shape, documented not engineered around):** `SearchHit` + `getArticle` expose only the bare ref, so a hit/deep-link at a duplicate-ref article can only land on the *first* eid — consistent with what citations carry.
- **Design principles (high-end-visual-design; structure/spacing/motion only):** ① Double-bezel enclosures → controls bar, document cards, masthead, panes (outer hairline shell `p-1.5` + recessed core). ② Earned depth, no gray borders → `border-foreground/[0.06–0.1]` hairlines + `shadow-card`/`shadow-card-hover`. ③ One easing language → only the `ease-spring` token (card lift, chevrons, view toggle) — 0 `ease-[cubic-bezier]`. ④ Magnetic micro-interactions → card `hover:-translate-y-0.5` + button-in-button arrow nudge, staggered `animate-fade-up` on the grid. ⑤ Macro-rhythm + uppercase-tracked eyebrows + generous spacing; the reader is a fixed-height "workbench" (tree | reading | XML) with independent per-pane scroll. ⑥ GPU-safe (transform/opacity/content-visibility) + `prefers-reduced-motion` honoured. (Font/icon/palette bans NOT followed — author-locked stack, same call as S7-FIX…S11.)
- **Verify (offline):** `npx tsc --noEmit`→**0**; `npm run build`→**11 routes**, `/corpus` 16.3 kB / 153 kB First-Load, prerendered static, standalone intact; `npm ls tailwindcss`⇒**3.4.19**. **API (temp backend `:8000` `KG_BUILD_ON_START=false`, torn down):** 45 documents; tree-grouping 0 mismatches (1055/660/78); `getArticle('art_1')`==`getArticle('1')`→eid art_1; cross-corpus "ميراث"→12 hits across docs, in-doc "القانون" scoped to 75-58→50 hits all in-doc. **Browser (isolated standalone :3100 → backend :8000, `--disable-web-security`, torn down):** library PNG = 45 cards w/ RTL titles + tone badges + format chips (absent PDF struck-through) + filters; reader PNG = tree ⟷ RTL reading ⟷ coloured AKN XML tree. **Deep-link DOM dumps:** `?doc=1963…&article=5` → `<article data-eid="art_5">` has `bg-primary/[0.05]`, XML `data-eid="art_5"` present, exactly 1 tree `aria-current`, 78 blocks; `?doc=75-58…&article=1` (Civil Code) → **1055 blocks**, art_1 selected, **pre-mode** "fast highlighted view" + XML `data-eid="art_1"`, 1 `aria-current`. **Scroll-to caveat:** bare-ref→eid resolution + selection sync across all three surfaces is browser-proven; the literal *scroll-offset* is verified by **logic only** (refs populate at commit before the effect fires; `settleScroll` rAF loop handles content-visibility drift) — headless captures **blank on far-scroll deep-links** (near-top art_5 renders 157 kB; far-scroll art_70 + the 6.9 MB Civil Code blank ~6.6 kB), a capture race with the rAF scroll, not an app bug. Author should eyeball one high-article deep-link (e.g. `?doc=1963_1963-12-08&article=70`) live to close it. (Civil Code PNG blank for the same reason; DOM dump confirms full render.)
- **Gotchas:** (1) ⚠ my `npm run build` **rewrote the shared `.next`** + copied `.next/static`+`public` into `.next/standalone/` for the :3100 test — if the user has a `next-server` on :3000 they may need to restart it (S9/S11 gotcha). The :3100 standalone + the :8000 backend I started were both **torn down**; nothing was on :3000/:8000 before, left clean. (2) Backend for `/corpus` needs **no KG** — start with `KG_BUILD_ON_START=false`. (3) Verified mostly via **DOM dumps** (PNGs only for library+reader) to respect the OOM budget; author may eyeball live light/presenter + RTL + the scroll-flash motion. (4) `content-visibility`/`contain-intrinsic-size` set via a cast (`as React.CSSProperties`) — csstype 3.x in this repo accepts them, tsc clean. (5) XML pre-mode token classes live as literal strings in `xml-highlight.ts` so Tailwind's JIT emits them; flash classes (`bg-primary/15`) likewise literal in `xml-pane.tsx`.
- **Next:** **S13 — RDF KG explorer `/kg`:** Cytoscape `fcose` bounded subgraph + node/edge-type + per-doc filters + node inspector; consumes `/kg/meta`·`/kg/subgraph`·`/kg/node`·`/kg/search` (typed client + `KGMeta/Subgraph/NodeDetail/KGSearchHit` already in `lib/{api,types}.ts`); **deep-link target `/kg?node=<id>`** from the CitationChip's secondary link; **NEVER load the 74 MB TTL** — query the prebuilt SQLite index. **First step:** `app/kg/page.tsx` (server → `<Suspense>` → client) + a `kgMeta` hook for the filter facets, then a Cytoscape mount fed by `kgSubgraph`.

## S13 KG explorer (2026-06-07)

- **Status:** done (frontend only; **no backend / `akn_rlm` / git**). Design skill `high-end-visual-design` invoked first — structure/spacing/motion only; author-locked Instrument-Serif/Inter/Plex-Arabic/Lucide + Ink-Orange-Gold kept (same call as S7-FIX…S12). Interactive, BOUNDED Cytoscape `fcose` explorer over the 765k-triple KG (80,560 nodes / 398,335 edges / 39 node-types / 69 predicates / 51 docs) — **never the 74 MB TTL**, only the SQLite-backed `/kg/*`.
- **Shipped:** `app/kg/page.tsx` (server `PageHeader`→`<Suspense>`→client). `components/kg/`: `palette.ts` (**pure, load-bearing**: probe-element colour resolution, type→role map, edge styling, label/format, curated default filter sets), `graph-canvas.tsx` (Cytoscape mount, `forwardRef`+`useImperativeHandle`), `filters.tsx` (mode·search·law·depth·max·node/edge toggles), `node-inspector.tsx`, `legend.tsx`, `kg-explorer.tsx` (URL shell + queries), `cytoscape-fcose.d.ts`. **Deps added:** `cytoscape@3.34.0` + `cytoscape-fcose@2.2.0` + `@types/cytoscape` (Tailwind held **3.4.19**, not v4). **Consumes:** `kgMeta`·`kgSubgraph`·`kgNode`·`kgSearch` (TanStack; Abortable via `signal`; lazy-expand uses its own `AbortController`). My files add **0** `ease-[cubic-bezier]` (7× `ease-spring`); lone build warning = pre-existing `components/architecture/*`.
- **Cytoscape SSR/theme:** canvas loads via **dynamic `import('cytoscape')`+`import('cytoscape-fcose')` inside `useEffect`** — never SSR-bundled → `/kg` prerenders **static** (25.4 kB). `cytoscape.use(fcose)` registered once (module flag). **Colours: NEVER feed raw CSS-var channels** (cytoscape's parser silently blacks out `hsl(37 96% 49%)`) — `resolveThemeColors()` reads each token off a throwaway probe `<span>` → canonical `rgb()` (auto theme+presenter aware); the stylesheet is rebuilt + `cy.style()` re-applied on `resolvedTheme` change. Nodes coloured by `type` (role map by global frequency rank → Orange/Blue/Gold/Green/Prussian/…; legend documents it), sized by in-view degree, structural types = rounded-rects; edges styled by predicate name-pattern (amend/repeal = dashed primary, refs = dotted info, containment quiet, `type` faint). Arabic labels render via canvas bidi + Plex-Arabic fallback. `fcose animate:false` under reduced-motion; container has a resolved fixed height (workbench) + `ResizeObserver`→`cy.resize()`.
- **Lazy-expand/merge + truncation:** single **tap = select** (→inspector + writes `?node=`); **double-tap / inspector "Expand neighbours" = expand** (`kgSubgraph({seed,depth,limit,…filters})`, merged by id-dedupe, fcose warm-start `randomize:false`) — **no auto-expand of high-degree**. Base graph is a prop; replacing it (filter/seed change) resets the canvas, expansions live in `cy`. `truncated`/`total_neighbors` → context-bar "capped · N more" chip + dashed-gold `has-more` ring on the seed + inspector note. **Overview defaults to STRUCTURAL node/edge types** (advisor) so a big law renders a clean article-around-hub skeleton, not a 250-node paragraph hairball; drill-down = all types minus the dominating `type` predicate.
- **`?node=` deep-link + /corpus back-link:** on init, `?node=` → drill seed + select + recentre + inspector. Selection writes `?node=` via `URLSearchParams` (encodes `#`/Arabic). **`selfNavRef` guard** distinguishes our own URL writes from genuine external nav so an in-graph click never re-seeds (fixed a real re-seed-on-second-click race; `sp` lags `selectedNodeId` by a render). Inspector → `/corpus?doc=<corpus_link.doc_id>&article=<corpus_link.article_ref>` (S12 contract). Invalid id → inspector ErrorState + empty canvas (graceful).
- **Verify (offline):** `npx tsc --noEmit`→**0**; `npm run build`→**11 routes**, `/kg` **static-prerendered** 25.4 kB; `npm ls tailwindcss`⇒3.4.19, `cytoscape@3.34.0`+`cytoscape-fcose@2.2.0`. **Backend up (existing SQLite index, no LLM, torn down after) + headless Chrome via raw CDP (Node global WS), prod-standalone :3100 → backend :8000 `--disable-web-security` (CORS allows only :3000), both + chrome torn down — read DOM state, not canvas pixels:** default overview = **58 nodes/122 edges bounded, 0 console errors**, Goldilocks law `05-01`; **Arabic deep-link** `?node=…#art_3_مكرر1` → mode `drill`, `selected` == exact id (round-trip OK), inspector + `corpusHref=/corpus?doc=02-03_2002-04-10&article=3_bis_1`; **additive merge** = real tap on Law hub (no re-seed, base stayed 11) → Expand → **+20 (11→31)**; **dedupe** = expand on a complete-ball node → **+0**; **truncation** = high-degree seed (deg 1476/248) → capped **exactly 250**, `truncated=true`; **theme toggle** → dark→light, 58 nodes retained, 0 errors. **Screenshot** (1440×900, light) = bespoke radial graph (orange hub + green article round-rects, Arabic labels, branded — **not** default-grey) + 3-pane workbench `/tmp/s13-shots/kg.png` (throwaway).
- **Design principles (high-end-visual-design; structure/spacing/motion only):** ① Double-bezel enclosures → rail/canvas/inspector shells (outer hairline `p-1.5` + recessed core + inset top-highlight). ② Earned depth, no grey borders → `border-foreground/[0.05–0.1]` hairlines + `shadow-card`; warm `primary` glow only on the selected node (cy overlay) + active mode/row. ③ One easing language → only the `ease-spring` token (mode/toggle/chip/arrow nudges) — 0 `ease-[cubic-bezier]`. ④ Magnetic micro-interactions → chip/btn `active:scale`, inspector corpus-arrow `group-hover:translate`, hover-dims the canvas to a node's neighbourhood. ⑤ Macro-rhythm + uppercase-tracked eyebrows + the workbench's independent per-pane scroll. ⑥ GPU-safe (transform/opacity) + `prefers-reduced-motion` (fcose `animate:false`). (Font/icon/palette bans NOT followed — author-locked stack, same call as S7-FIX…S12.)
- **Gotchas:** (1) ⚠ my `npm run build` **rewrote the shared `.next`** (+ staged `.next/static`/`public` into `.next/standalone` for the :3100 test) — if the user has a `next-server` on :3000 they may need to restart it (S9/S11/S12 gotcha). The :3100 standalone + the :8000 backend **I started** were both **torn down**; nothing was on :3000/:8000/:9222 before, left clean. (2) **/corpus back-link resolution wrinkle (backend dual-representation, NOT an S13 bug):** numeric refs (`article=5`, the overwhelming citation case) resolve to the eId directly; but a KG `corpus_link.article_ref` is romanised (`3_bis_1`) while S12's corpus uses Arabic AKN eIds (`art_3_مكرر1`), so a transliterated ref can fall to **doc-top** via S12's documented `resolveRefToEid`→`getArticle` fallback. We pass the contract faithfully and can't touch the backend; consistent with [[project-corpus-article-ref-non-unique]]. **Reasoned, not browser-confirmed** (backend torn down). (3) **Backend-down clean state + invalid `?node=` empty state = wired + reasoned, not browser-verified** — both ride the standard `metaQuery→ErrorState` / empty-base paths proven on corpus/benchmark. (4) `type` predicate dominates edges (93,736) → **default-excluded**; included in the filter list to opt back in. (5) cytoscape string `'data(x)'` mappers fail tsc for non-string props → used **function mappers**; cast layout opts `as unknown as LayoutOptions` (fcose name not in the union). (6) No-eslint repo + pre-ES2015 tsc target → avoided `Set` spreads (`new Set(set)`/`forEach`, not `[...set]`).
- **Next:** **S14 — About page `/about`:** editorial page from thesis front matter (title, motivation, authors Attia & Chaoui, ENSIA, jury + viva 13/06/2026, the two novel resources — AlgerianLegalBench + the AKN/RDF corpus — AI-usage note); mostly static content via `lib/about.ts`. **First step:** pull the front-matter copy from `thesis/Thesis.txt` into a typed `lib/about.ts`, then a server-rendered editorial layout reusing `PageHeader` + the double-bezel card idiom.

## S14 About page (2026-06-08)

- **Status:** done (frontend only; **no backend / `akn_rlm` / deps / git**). Design skill `high-end-visual-design` invoked first — structure/spacing/motion only; author-locked Instrument-Serif/Inter/Plex-Arabic/Lucide + Ink-Orange-Gold kept (same call as S7-FIX…S13). The jury-facing editorial "face": masthead → motivation → system-in-brief + headline metrics → the two novel resources → authors & committee → demo/AI-usage honesty → explore/colophon. **Renders fully OFFLINE** (static first paint from `lib/about.ts`); `/api/meta` is a SILENT enrichment only.
- **Shipped:** `lib/about.ts` (typed content module — single source of truth) + `app/about/page.tsx` (`'use client'`; SSR-prerendered **static**), replacing the S0 `ComingSoon` stub. No new components/deps; reuses `AuroraBackdrop`·`ArabicText`·`Badge`·`fmtInt`·`usePrefersReducedMotion`. My files add **0** `ease-[cubic-bezier]` (only `ease-spring`); lone build warning = pre-existing `components/architecture/*`.
- **Content provenance (every fact REAL, from `thesis/Thesis.txt` lines 1–133):** **title** verbatim; **authors** Ibrahim El Khalil Attia & Maab Chaoui; **ENSIA** (École Nationale Supérieure d'Intelligence Artificielle) + Dept. of Intelligent Systems Engineering + degree + **FYP/2025/135**; **viva 13/06/2026**; **jury** (linearized 4-row table, lines 22–48 — triple-locked by position: name/grade/country blocks all align, Cantador the only non-ENSIA/non-Algeria entry pins the rest): **Wahid Chami** (MCA, ENSIA, Algeria) — President · **Fouad Dahak** (MCA, ENSIA) — Supervisor · **Ivan Cantador** (Professor, U. Autónoma, Spain) — Co-supervisor · **Mohamed Brahimi** (MCA, ENSIA) — Examiner. "MCA"/"U. Autónoma" kept verbatim (NOT expanded to Madrid). **Abstract** (104–133)→MOTIVATION; **AI-usage disclosure** (84–98) summarised + an honest demo-built-with-AI note. **Metrics** = locked Phase-E/thesis §4: Cite F1 **0.3045**, HCR **0.000**, JIR **0.000**, Abstention F1 **0.703**; improvements **1.64× / 2.9× / 1.74×** (tiles link → /results). **Resources:** AKN corpus + AlgerianLegalBench v3.0 (244 q · 23 cats · 8 types · 40 jurisdictional-infection traps). Arabic accent = single safe term **القانون الجزائري** (gloss "Algerian law").
- **`/api/meta` (live, offline-safe):** `useQuery(['meta'])` (shared cache, `signal`/AbortController, `retry:false`) feeds the 4 headline counts + 2 resource counts via `statValue(meta,stat)` — live value only if a **finite number** (guards `kg_triples: number|null`), else the baked-in **STATIC fallback** (**45 docs / 8,868 articles / 244 questions / 765,000 triples**, consistent with /corpus·/benchmark·/kg). **No skeleton / no ErrorState / no OfflineBanner** here; backend down → static numbers stay (no layout shift, no crash; `providers.tsx` QueryClient has no `throwOnError`/suspense). Thesis "171 source files (1963–2025)" is shown DISTINCTLY as a labelled fact, NOT as the document count.
- **Design (high-end-visual-design; structure/spacing/motion only):** ① `Bezel` double-bezel enclosures (outer hairline `p-1.5` tray + recessed inner core) on resources/jury/author/motivation/AI panels. ② Editorial masthead: huge Instrument-Serif hero (`HERO_LINE`), eyebrow pills, cited dissertation-title block. ③ Macro-whitespace (`pt-24/28` per section). ④ One easing language `ease-spring`; button-in-button `CtaLink` with magnetic trailing-icon nudge. ⑤ Scroll-reveal `Reveal` (IntersectionObserver, transform+opacity, staggered) — masthead uses house `animate-fade-up` so first paint is NEVER blank; reduced-motion → instant show. ⑥ GPU-safe; RTL only on the Arabic accent, chrome LTR. (Font/icon/palette bans NOT followed — author-locked stack, same call as S7-FIX…S13.)
- **Verify (offline):** `npx tsc --noEmit`→**0**; `npm run build`→**11 routes**, `/about` **static-prerendered** 11.5 kB / 125 kB First-Load; `npm ls tailwindcss`⇒**3.4.19**. **Static first paint proven by grepping `.next/server/app/about.html`** (backend NOT involved): all author+jury names, `13/06/2026`, `Co-supervisor`, `U. Autónoma`, `FYP/2025/135`, `0.3045`, `0.703`, `القانون الجزائري`, AND the static counts `45 / 8,868 / 244 / 765,000` ALL present. **Browser screenshot skipped** to respect the OOM budget — offline render proven structurally via the prerendered HTML + the no-throw QueryClient.
- **TODO(author) flagged:** (1) external links (paper / code repo / ENSIA URL) — left as a comment in `lib/about.ts`; none hard-coded, so nothing 404s in the viva. (2) a richer composed Arabic tagline — only the single safe term ships; have the Algerian author review before adding longer Arabic to this jury-facing surface. Everything else is sourced; nothing fabricated.
- **Gotchas:** (1) ⚠ my `npm run build` **rewrote the shared `.next`** — a `next-server` on :3000 may need a restart (S9/S11/S12/S13 gotcha). I started NO backend and bound NO ports; :3000/:8000 left untouched. (2) page is `'use client'` (can't `export const metadata`) — global metadata from `app/layout.tsx` still applies; chose the 2-file deliverable over a server-page+client-island split. (3) `Reveal` starts below-fold sections at `opacity-0` (revealed on scroll/mount) — "offline" here = backend down (JS still runs) so content is always reachable; above-fold + reduced-motion show instantly. (4) thesis abstract rounds Cite F1 to "0.305"; the page shows **0.3045** (locked run, matches /results) — same number, un-rounded.
- **Next:** **S15 — Live hardening + REAL health probe:** implement the backend `health.llm` probe so Live un-gates when the LLM endpoint is reachable (today hardcoded `"unchecked"` — see [[project-live-mode-health-gate]]), startup pre-warm, `LIVE_TF_CD` replay|live, nearest-precomputed fallback, SSE reconnect/toasts. **First step:** replace the hardcoded `health.llm` in `backend/app/.../health.py` with a real, fast, cached reachability check.

## S15 Live hardening + health probe (2026-06-08)

- **Status:** done (backend reliability + small frontend wiring; **no `akn_rlm` edit, no deps, no git**). Live mode was BUILT+correct (S10e) but DARK everywhere because `health.py` hardcoded `"llm":"unchecked"`. S15 makes Live actually un-gate when the LLM endpoint is reachable, and hardens the hybrid behaviour — **offline-first stays sacred** (nothing added crashes/hangs/drifts a metric when offline). All 5 acceptance items PASS.
- **Shipped (backend):** NEW `app/services/health_probe.py` (`get_llm_status()` + `_probe_once()` + `reset_health_cache_for_tests()`); `health.py` `llm` field now = the real cached probe; `deps.py` `warm_services()` extended (`prewarm_default_dispatcher` + `prime_health_probe`); `pipeline.py` (`LiveReplayRedirect`, `TF_CD_TYPES`, `_safe_classify`, LIVE_TF_CD guard in `answer()`); `benchmark.py` `nearest_prediction()`; `answer.py` router (catch redirect in sync `/answer` + SSE `_live_events` → `_fallback_replay_events`; NEW `GET /api/answer/nearest`); `settings.py` (`WARM_DISPATCHER_ON_START`, `LIVE_TF_CD`, `LLM_PROBE_TIMEOUT_S`, `LLM_PROBE_CACHE_TTL_S`); `models/answer.py` `NearestResponse`. NEW tests: `test_health_probe.py`(12), `test_prewarm.py`(2), `test_nearest_prediction.py`(5), `test_live_tf_cd.py`(8). `conftest.py`: `WARM_DISPATCHER_ON_START=false` + an autouse `_reset_sse_app_status` fixture.
- **Shipped (frontend):** `utils.ts` `isLlmReachable` narrowed to **only `llm==='ok'`**; `types.ts` `LlmStatus` enum + `Health.llm:LlmStatus` + `ReplayFallback`/`NearestExample` + `AnswerResponse.fallback?`/`DoneEvent.fallback?`; `api.ts` `getNearest()`; `sse.ts` transport-drop **retry** (`streamOnce`+wrapper, `onReconnect` cb, cap live=1/replay=2, server-`error`/HTTP-error terminal, abort-safe); `use-pipeline-stream.ts` `RECONNECT` action + `onReconnect` opt; `app/page.tsx` reconnect toast + `onReplayInstead`→`getNearest` replays the **nearest** Q + once-per-run fallback toast + persistent in-panel `FallbackBanner`. NEW `lib/sse.retry.test.ts`(3).
- **Probe contract (`health.llm` ∈ `ok|unreachable|disabled|unchecked`):** reads `AI_GRID_BASE_URL`/`AI_GRID_API_KEY` from `akn_rlm.config` (read-only; `os.getenv` freshness fallback). `OFFLINE_MODE` **or** no key/base → `"disabled"` instantly (no network, exact). Else GET `{base}/models` w/ Bearer, **2.5 s** timeout — **only 2xx → `"ok"`** (401/4xx/5xx/timeout/exc → `"unreachable"`; a rejected key never un-gates Live). Cached **20 s**. `/health` is **non-blocking**: cheap "disabled" inline; the HTTP probe runs on a **daemon thread** (≤1 in flight) and `/health` returns last-known / `"unchecked"` the first online call → never hangs (test sleeps 5 s in the probe; `get_llm_status()` returns `<1 s`). `_probe_once` is pure (all inputs explicit) → unit-testable; **never raises**.
- **⚙ ENABLE-LIVE RECIPE (viva machine):** set **BOTH** `OFFLINE_MODE=false` **AND** `AI_GRID_API_KEY=<key>` (and optionally `AI_GRID_BASE_URL`). `OFFLINE_MODE=true` (the dev/test/compose default) deterministically returns `"disabled"` **even with a valid key** — by design. With both set + a reachable gateway, `/health.llm→"ok"` and the composer/Live switch flips on (`isLlmReachable` ⇔ `llm==='ok'`, both ends agree).
- **LIVE_TF_CD (default `replay`) + fallback (live free-form ONLY — never the locked offline run, zero metric drift):** `answer()` classifies first (one cheap call; result passed into `run` so no double-classify); a `temporal_factual`/`conceptual_definitional` result raises `LiveReplayRedirect` **before** any dispatcher build → the 74 MB rdflib KG is **never** loaded. Redirect is a plain `Exception` (NOT `PipelineLiveError`) so the `except Exception→PipelineLiveError` wrap can't swallow it; routers catch it first. SSE serves the nearest precomputed example inline as a replay carrying `fallback{reason,note,example_question_id}` on `answer`+`done` (`mode:"replay"`); sync `/answer` returns the nearest replay (503 only if no predictions). `LIVE_TF_CD=live` disables the guard (real KG path). `_safe_classify` swallows a down-LLM to `None` → falls through to `run` → real `PipelineLiveError` (a down LLM is never misread as a TF/CD redirect — protects existing `test_live_model_failure`).
- **⚠ KG-off BACKSTOP (closes a real hole, NOT cosmetic):** classify-first alone is NOT airtight. Our `classify` uses `llm_classify(fallback_to_regex=False)` → an *unparseable* (but reachable) LLM label yields confidence 0.0 → `_safe_classify`→`None` → no redirect. But the **dispatcher's** `make_llm_classifier_fn` uses `fallback_to_regex=**True**` → on the same unparseable label it falls back to **REGEX**, whose `_TEMPORAL_FACTUAL_KW`/`_CONCEPTUAL_KW` rules emit TF/CD (conf 1.0) → `kg_loader` would load the 74 MB KG on the viva box. Fix (`pipeline.py:answer`): when the type is genuinely undetermined under `replay` (`query_type is None`), build the live dispatcher with **`use_kg=False` (kg_loader=None)** so an undetected TF/CD **abstains** instead of loading the KG. Surgical — fires ONLY on ambiguity; confidently-routed runs keep their KG/enhancer behaviour. `prewarm_default_dispatcher` builds the matching `use_kg = (LIVE_TF_CD != "replay")` variant so the common live auto-query reuses the warmed dispatcher. (Tested: `test_undetected_type_forces_kg_off_backstop` asserts `kg_loader is None`.)
- **Verify (offline + mock-live, venv, sandbox OFF):** `.venv/bin/python -m pytest backend/tests -m "not live"` ⇒ **55 passed, 1 skipped, 3 deselected** (incl. all NEW: probe→disabled/unreachable/ok+cached+never-hangs; pre-warm builds dispatcher w/o LLM + offline-safe; LIVE_TF_CD redirect + the **kg-off backstop** for undetected types + SSE fallback note; nearest-by-type+lexical). `curl`-equiv `/api/health` via the venv app (OFFLINE) ⇒ `"llm":"disabled"` (real field; **not `"ok"`**); `/health.llm→"ok"` path locked by a monkeypatched endpoint test. Frontend: `tsc --noEmit`→**0**, `npm run build`→**11 routes** OK, `vitest run`→**13 passed** (incl. sse retry: reconnect-then-complete / server-error-no-retry / retries-exhausted), Tailwind **3.4.19**. `git status akn_rlm/` clean.
- **Gotchas:** (1) ⚠ `npm run build` rewrote the shared `.next` — a `next-server` on :3000 may need a restart (S9+ gotcha). I started NO server, bound NO ports; user's :3000/:8000 untouched (their :8000 still runs pre-S15 code until restarted). (2) **conftest `WARM_DISPATCHER_ON_START=false` is load-bearing** — without it every `TestClient(app)` lifespan would pull real BM25+dense (download/OOM/hang). (3) the autouse `_reset_sse_app_status` fixture clears sse-starlette's process-global `AppStatus.should_exit_event` between tests — S15 added a 2nd real SSE stream test, which otherwise crashes the 1st with "Event bound to a different event loop". (4) classify-first adds ONE extra cheap classifier call per auto live query under `LIVE_TF_CD=replay` (acceptable; live-only). (5) the `{base}/models` 2xx⇒ok gate is UNTESTED against the real AI-Grid gateway (offline here) — port 4000 is litellm (200 on `/v1/models`); if it 404s there, swap `_probe_once` to a HEAD/chat ping. See [[project-live-mode-health-gate]].
- **Next:** **S16 — Docker finalize:** pre-bake HF weights into the image (e5-small, cross-encoder, mDeBERTa) so `WARM_DISPATCHER_ON_START=true` pre-warm + dense never hit the network; KG SQLite index built idempotently at entrypoint; read-only mounts; mem limits + healthchecks w/ generous `start_period` (pre-warm now loads BM25+dense at startup → first `/health` may lag); `NEXT_PUBLIC_API_BASE` build arg; clean-machine `docker-compose up` E2E + demo seed + README (incl. the ENABLE-LIVE recipe above). Do NOT touch git.

## S15-FIX Live health gate diagnosis (2026-06-08)

- **Status:** partial external blocker; config/docs fixed, real Live still blocked by expired AI-Grid key. No `akn_rlm` edit, no deps, no git.
- **Root cause:** actual `curl -s localhost:8000/api/health` returned `{"offline_mode":true,...,"llm":"disabled"}`. Runtime import showed `settings.OFFLINE_MODE=True`, AI-Grid base present, key present. After forcing `OFFLINE_MODE=false`, `get_llm_status(block=True)` returned `"unreachable"` because the real AI-Grid endpoint rejected the configured key: direct `/v1/models` and `/v1/chat/completions` probes both returned HTTP 400 with an `expired_key` error. This is not a frontend bug and not a `/models` path bug; Live must stay gated until a valid, non-expired key is supplied.
- **Shipped:** `docker-compose.yml` keeps offline-first default but now honors explicit live overrides (`OFFLINE_MODE=${OFFLINE_MODE:-true}`, `AI_GRID_API_KEY`, `AI_GRID_BASE_URL`); `.env.example` documents the live contract; `backend/README.md` now uses the required project venv and includes exact host/compose live commands.
- **Verify:** `/home/maab/Documents/pfe/methodology/PFE_locally/.venv/bin/python -m pytest backend/tests/test_health_probe.py -m "not live" -q` ⇒ `12 passed`. Current running backend still correctly returns `"llm":"disabled"` because it is the offline-mode instance. Direct key probes: `/models` ⇒ 400 `expired_key`; `/chat/completions` ⇒ 400.
- **Live run command (host, after replacing `.env` with a valid key):** `cd /home/maab/Documents/pfe/methodology/PFE_locally && set -a && source .env && set +a && cd backend && OFFLINE_MODE=false AI_GRID_BASE_URL="${AI_GRID_BASE_URL:-http://app.ai-grid.io:4000/v1}" /home/maab/Documents/pfe/methodology/PFE_locally/.venv/bin/python -m uvicorn app.main:app --reload --port 8000`; then `curl -s localhost:8000/api/health` twice if the first online call is `"unchecked"`; expected `"llm":"ok"` only with the fresh key.
- **Next:** replace the expired AI-Grid key, restart the backend with `OFFLINE_MODE=false`, rebuild/restart frontend if `.next` is stale, then verify browser Network receives `llm:"ok"` and a short non-TF/CD live query streams.

## S15-FIX2 Root start script (2026-06-08)

- **Status:** done. No `akn_rlm` edit, no deps, no git.
- **Shipped:** root `start.sh` executable. It loads root `.env`, requires `AI_GRID_API_KEY`, forces host-local dataset/index paths, starts backend live-mode (`OFFLINE_MODE=false`, venv uvicorn on `:8000`) and frontend (`npm run dev` on `:3000`), and traps `Ctrl+C`/TERM/EXIT to kill both process groups (uvicorn reloader + Next child included). Defaults keep `LIVE_TF_CD=replay` and `KG_BUILD_ON_START=false` to avoid the heavy live KG path/startup work; overrides still allowed via env.
- **Verify:** `bash -n start.sh` passed; `chmod +x start.sh` applied. Did not full-run because a backend was already bound on `:8000` and the current AI-Grid key is still expired.
- **Next:** replace the expired key in `.env`, run `./start.sh`, verify `curl -s localhost:8000/api/health` returns `"llm":"ok"` (repeat once if first call is `"unchecked"`), then click Live and run a short non-TF/CD query.
