[pytest]
markers =
    live: tests that require a reachable live LLM endpoint (keys in akn_rlm/.env). Skipped by default via `-m "not live"`.
    models: smoke tests that require cached local embedding or cross-encoder weights.
